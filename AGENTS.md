# IVSBox 项目开发规则

> 适用目录：`f:\debug\T113_TCP`
> 主工程：`ivsbox-v5/`
> 建立日期：2026-09-22（2026-09-23 增补规则 4、规则 5、规则 6、规则 7、规则 8）

本文件是**强制规则**，所有对本项目的开发、修改、文档编写都必须遵守。

---

## 规则 1：功能实现后必须回写文档

新建的 `.md` 文档必须维护"实现记录"章节。

具体要求：

1. 每完成一个功能点，在对应 `.md` 文档中新增一行记录。
2. 记录内容至少包含：**功能名称、实现状态、完成时间（YYYY-MM-DD HH:mm）**。
3. 时间以本机当前时间为准，不得填写预计时间或占位时间。
4. 功能从"未实现"变为"已实现"时，同步更新状态列。
5. 文档格式建议如下：

```markdown
## 实现记录

| 功能 | 状态 | 完成时间 | 说明 |
|---|---|---|---|
| 交叉编译工具链打通 | 已实现 | 2026-09-22 17:30 | ARM 二进制能在板端启动 |
| Reactor 事件循环 | 未实现 | — | 计划 S03 |
```

---

## 规则 2：所有修改必须登记历史记录

**唯一权威记录文件**：`ivsbox-v5/docs/修改记录.md`
（即需求口径中的 `xiugaijilu.md`，**不另建同名文件**，记录一律写进上面这个文件）

具体要求：

1. **任何一次代码或文档修改**，都必须在该文件末尾追加一条记录。
2. 每条记录至少包含三要素：
   - **修改的文件**：列出全部被改动的文件路径
   - **问题 / 目的**：这次修改解决什么问题、达成什么目的
   - **时间**：YYYY-MM-DD HH:mm
3. 采用倒序还是正序由该文件自身规定，**新增记录一律追加，不覆盖历史**。
4. 一次修改涉及多个文件时，写为一条记录，不要拆成多条。
5. 禁止修改或删除已存在的历史记录条目；若需更正，只能新增一条更正记录。
6. 记录模板：

```markdown
## 2026-09-22 17:30

- **修改文件**
  - `src/hal/iv_serial.c`
  - `include/ivsbox/iv_serial.h`
- **问题 / 目的**：串口 raw 模式未关闭流控，导致收到数据被内核加工，帧解析错位。
- **处理方式**：在 `iv_serial_open()` 中显式关闭 CRTSCTS，设置 VMIN=0 / VTIME=0。
- **验证结果**：用 socat 虚拟串口灌入 1000 帧，解析成功率 100%。
```

---

## 规则 3：修改成功后必须提交并推送 git

**远端仓库地址**：`https://github.com/ppp1234lll/T113_S4`（分支 `main`）

具体要求：

1. **任何一次修改成功后**（代码、文档、脚本一律适用，不再限于"修复问题"），必须执行一次提交并推送到上述远端仓库备份。
2. 提交前先登记规则 2 的历史记录，**历史记录与代码在同一次提交中**；有代码改动时同时补规则 4 的代码说明。
3. 提交信息格式（遵循 Conventional Commits）：

```text
<type>(<scope>): <简短描述>

<详细说明，可省略>
```

- `type` 取值：`feat` 新功能 / `fix` 修复 / `docs` 文档 / `refactor` 重构 / `test` 测试 / `build` 构建 / `chore` 杂项
- `scope` 取值：模块名，如 `hal`、`link`、`proto`、`netmgr`、`media`、`web`
- 示例：`fix(hal): 关闭串口流控，修正帧解析错位`

4. 推送前确认远端已配置（`git remote -v` 应存在 `origin` 指向上述地址），禁止强制推送到主分支。
5. 提交范围只包含本次修改涉及的文件，**不要用 `git add -A` 或 `git add .`**，避免误提交构建产物和临时文件。
6. 若本次修改涉及密钥、口令、设备序列号等敏感内容，**禁止提交**，必须先过滤。
7. 推送失败（无权限、冲突、网络异常）**必须明确告知用户失败原因和待处理项**，不得静默跳过、不得假装已推送。

---

## 规则 4：每次写代码后必须交付"代码说明"

**记录文件**：`ivsbox-v5/docs/代码说明.md`

具体要求：

1. 只要有代码产出（新增或修改 `.c` / `.h` / `.sh` / `.py` 等），写完后必须在该文件追加一节。
2. 每一节至少覆盖四点：
   - **干了什么事**：改了哪些文件、哪些函数、关键逻辑与数据流
   - **解决什么问题**：改动前的现象或缺口，改动后的行为
   - **影响范围**：会被影响的其他模块、接口、配置，以及未覆盖的风险 / TODO
   - **验证方式与结果**：编译、单测、板端实测命令与输出
3. 目的：**不看代码也能知道干了什么事、解决了什么问题**。
4. 纯文档修改（不含代码）不必写代码说明，只按规则 2 登记即可。

---

## 规则 5：优先参考天嵌官方资料

**天嵌官方资料入口**：`https://embedsky-rd.yuque.com/nqqg7d/fztngl`

遇到 T113 / 天嵌（embedsky）平台相关问题时，查找顺序固定为：

1. **先查天嵌官方语雀资料**（上述地址，含板卡手册、SDK 说明、系统配置、外设调试等）。
2. 其次查天嵌 SDK 源码、板端实际文件与官方示例（以板端实际为准）。
3. 官方资料确实不覆盖时，才寻找其他公开资料；此时必须**标注来源**，并说明为何官方资料不适用。

严禁凭猜测编造寄存器、引脚、分区、设备节点、工具链等平台事实；查不到就写"待确认"。

---

## 规则 6：处理问题结束后不得遗留任何临时文件

**原则：任务收尾时工作区只保留最终文件。**

具体要求：

1. 处理问题过程中产生的一切临时物，任务结束前必须清理干净，包括：
   - 临时脚本、一次性测试程序、调试用 `main` 函数
   - 调试日志、抓包文件、寄存器 dump、串口原始日志
   - 备份与中间文件：`*.bak`、`*.orig`、`*.tmp`、`*.old`、`*~`、`*.rej`、`*.patch`（仅调试用）
   - 被替换掉的旧版本文件、复制的副本、试验用目录
2. **不许用"注释掉"代替删除**：试错的代码、被否掉的实现一律删掉；需要回溯就靠 git 历史，不要留在文件里。
3. 构建产物（`build/`、`*.o`、`*.exe`、`_*.txt` 等）不入库，并确认已被 `.gitignore` 覆盖。
4. 提交前必须用 `git status` 自检：不得出现本次任务之外的未跟踪文件；确需保留的调试证据，必须**归入约定目录并在规则 2 记录里写明用途和删除条件**，或明确告知用户保留原因。
5. 清理动作本身也要在规则 2 的修改记录中体现（删了哪些临时文件）。

---

## 规则 7：多 AI 协同必须共用同一份文档与同一套代码

**适用背景**：本项目同时由多个 AI 助手（如 WorkBuddy、TRAE 等）操作**同一个项目目录** `f:\debug\T113_TCP`。

**核心原则：无论哪一个 AI 处理问题，都必须基于同一份文档、同一套规则、同一份代码，禁止任何一方另起一套。**

具体要求：

1. **文档唯一性**：所有 AI 一律使用本仓库中已存在的权威文档，不得各自维护平行副本：
   - 规则：根目录 `AGENTS.md`（本文件，双方共用）
   - 修改记录：`ivsbox-v5/docs/修改记录.md`
   - 代码说明：`ivsbox-v5/docs/代码说明.md`
   - 功能开发列表：`ivsbox-v5/docs/IVSBox-功能开发列表.md`
   - 架构与缺口：根目录两份架构文档
2. **新建文档必须先征得用户同意**：确有必要新建文档时，**不得直接创建**，须先向用户说明"为什么现有文档承载不了、新文档的定位与归属"，经用户确认后方可新建；新建后必须登记进本文件"关联文档"表。
3. **规则与代码同源**：所有 AI 遵循同一套 `AGENTS.md` 规则、同一份代码基线与编码规范（如 `ivsbox-v5/.clang-format`、`.gitattributes`），不得各自引入独立规则文件或本地约定。
4. **防覆盖（重要）**：多个 AI 可能同时编辑同一文件。**每次写入或追加前，必须先读取目标文件的最新内容**，在其基础上增量修改；严禁基于过期内容整段覆盖，防止把另一方刚写入的记录冲掉。
5. **单写者约定**：同一时刻尽量只让一个 AI 写同一份文档；确需并行时，双方各自只改互不重叠的章节，并在规则 2 的记录中写明改动范围。
6. **同步基线**：每次开工前先 `git fetch origin` 对齐远端（`main`），确认工作副本为最新且无未提交冲突后再动手；收尾时按规则 3 提交推送，使另一方下次开工即可拉到最新状态。
7. **冲突处理**：若发现目标文件已被另一方改动（`git status` 显示非本人改动、或文件内容与上次读取不一致），**先停下并向用户说明**，确认由谁处理、如何合并，禁止强行覆盖。

---

## 规则 8：用 git-stint 会话隔离，禁止在 main 工作副本直接改文件

> 规则 7 靠人工约定防冲突，没有工具级隔离。本规则用 `git-stint`（已全局安装，v0.6.1）
> 给**每个 AI 会话**开独立分支 + 独立 worktree，从物理上保证两个 AI 不会写到同一份文件。

**会话命名**（用前缀区分归属，便于对方识别）：

- TRAE 用 `trae-<主题>`，例：`trae-netmgr`
- WorkBuddy 用 `wb-<主题>`，例：`wb-proto`

**开工**：

1. `git fetch origin`；`git status -sb` 必须干净且与 `origin/main` 一致（有 ahead/behind 先按规则 7 第 7 条处理）。
2. `git stint start <前缀>-<主题>`，然后 `cd .stint/<会话名>/`。**本次任务的所有改动只在这个 worktree 内进行**，主工作副本 `f:\debug\T113_TCP` 只用于拉取与合并。
3. 改动文件用 `git stint track <文件...> --session <会话名>` 登记进待提交列表。**不 track，`conflicts` 就检测不到重叠。**
4. `git stint conflicts --session <会话名>` 检查是否与另一会话改了同一文件；**发现有重叠就停下、告知用户**，由用户决定谁继续，不得自行抢改。

**会话中**：规则 1 / 2 / 4 / 6 照常执行，改动（含共用文档）一律写在会话 worktree 内。

**收尾（顺序固定）**：

1. `git stint commit -m "<Conventional Commits 信息>"` → `git stint squash -m "<最终提交信息>"`，把会话内多次提交压成一个干净提交。
2. `git fetch origin`，再合并回 main（工具的 `git stint merge`，或常规 git merge）。**若 `AGENTS.md`、`ivsbox-v5/docs/修改记录.md`、`ivsbox-v5/docs/代码说明.md` 这类共用文档冲突，处理原则是"双方记录都保留、按时间倒序排列"**，绝不丢弃对方的条目。
3. `git stint end`（放弃则 `git stint abort --session <会话名>`）清理会话分支与 worktree；确认 `git stint list` 为 `No active sessions`、`.stint/` 下无残留（规则 6）。
4. `git push` 推 main（规则 3），让另一方下次开工能直接拉到最新。

**已知限制（本机实测）**：

- 多会话并存时，即使已 `cd` 进 worktree，本工具在 Windows 上仍会报 `Multiple active sessions`，因此**所有 stint 子命令都显式带上 `--session <会话名>`**。
- `git stint install-hooks` 只适配 Claude Code；**TRAE 没有对应 hook，必须自己手动执行 start / track / commit / squash / end**，不得跳过。

**禁止**：

- 禁止在 main 工作副本上直接编辑代码或文档（例外见下）。
- 禁止两个 AI 同时操作同一个会话或同一个 `stint/*` 分支。
- 禁止用 `git checkout --force`、`git reset --hard`、`git push -f` 去"解决"冲突。

**例外**：以下三类改动允许在 main 工作副本直接进行，但**必须在规则 2 的记录中写明原因**：

1. **规则文件自身**：`AGENTS.md`（本文件）的修改。
2. **仓库根的工具配置文件**：如 `.stint.json`、`.claude/` 等。原因是这些文件由工具从 main 根目录**直接读取**，而 `adopt_changes: "always"`（见 `.stint.json`）会把 main 上未提交/未跟踪的文件 stash 后搬进新会话 worktree —— 放进会话反而会让配置被搬离 main，导致另一方的 hook 与隔离策略瞬时失效。
3. **紧急修复**：必须能在记录中说明"为什么不能先开会话"。

---

## 规则执行检查表

每次完成任务后，逐项自查：

- [ ] 功能是否已回写到对应 `.md` 文档的"实现记录"章节（规则 1）
- [ ] 是否已在 `docs/修改记录.md` 登记本次修改的文件、问题、时间（规则 2）
- [ ] 是否已提交并推送到 `https://github.com/ppp1234lll/T113_S4`（规则 3）
- [ ] 提交信息是否符合 Conventional Commits 格式
- [ ] 提交的文件是否只包含本次修改，未夹带构建产物和敏感信息
- [ ] 有代码改动时，是否已在 `docs/代码说明.md` 写明"干了什么事、解决什么问题、影响范围、验证结果"（规则 4）
- [ ] 涉及 T113 / 天嵌平台事实时，是否已先查天嵌官方语雀资料并标注出处（规则 5）
- [ ] 临时文件、调试脚本、被否掉的代码是否已全部清理，工作区只剩最终文件（规则 6）
- [ ] 写入共用文档前是否已读取其最新内容、是否新建了未经用户同意的文档（规则 7）
- [ ] 是否用 `git stint start` 开了独立会话、收尾是否 `squash` + 合并 + `end` 清理（规则 8）

---

## 关联文档

| 文档 | 路径 | 说明 |
|---|---|---|
| 系统架构 | `T113运维终端-系统架构设计-v5-整合版.md` | 架构基线 |
| 缺口与风险清单 | `T113运维终端-v5架构缺口与风险清单.md` | G1~G19 |
| 功能开发列表 | `ivsbox-v5/docs/IVSBox-功能开发列表.md` | S01~S20 开发步骤 |
| 修改记录 | `ivsbox-v5/docs/修改记录.md` | 规则 2 的权威记录文件（即 `xiugaijilu.md`） |
| 代码说明 | `ivsbox-v5/docs/代码说明.md` | 规则 4 的权威记录文件 |
| 开发规则 | `AGENTS.md` | 规则 7 的权威规则文件，**所有 AI 共用同一份** |
| 编辑器格式化 | `ivsbox-v5/.clang-format` | 代码风格基线，所有 AI 共用 |
