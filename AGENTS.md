# IVSBox 项目开发规则

> 适用目录：`f:\debug\T113_TCP`
> 主工程：`ivsbox-v5/`
> 建立日期：2026-09-22（2026-09-23 增补规则 4、规则 5）

本文件是**强制规则**，所有对本项目的开发、修改、文档编写都必须遵守。

---

## 规则 1：功能实现后必须回写文档

新建的 `.md` 文档必须维护"实现记录"章节。

具体要求：

1. 每完成一个功能点，在对应 `.md` 文档中新增一行记录。
2. 记录内容至少包含：**功能名称、实现状态、完成时间（YYYY-MM-DD HH:mm）**。
3. 时间以本机当前时间为准，不得填写预计时间或占位时间。
4. 功能从"未实现"变为"已实现"时，同步更新状态列。
5. 文档格式建议如下：

```markdown
## 实现记录

| 功能 | 状态 | 完成时间 | 说明 |
|---|---|---|---|
| 交叉编译工具链打通 | 已实现 | 2026-09-22 17:30 | ARM 二进制能在板端启动 |
| Reactor 事件循环 | 未实现 | — | 计划 S03 |
```

---

## 规则 2：所有修改必须登记历史记录

**唯一权威记录文件**：`ivsbox-v5/docs/修改记录.md`
（即需求口径中的 `xiugaijilu.md`，**不另建同名文件**，记录一律写进上面这个文件）

具体要求：

1. **任何一次代码或文档修改**，都必须在该文件末尾追加一条记录。
2. 每条记录至少包含三要素：
   - **修改的文件**：列出全部被改动的文件路径
   - **问题 / 目的**：这次修改解决什么问题、达成什么目的
   - **时间**：YYYY-MM-DD HH:mm
3. 采用倒序还是正序由该文件自身规定，**新增记录一律追加，不覆盖历史**。
4. 一次修改涉及多个文件时，写为一条记录，不要拆成多条。
5. 禁止修改或删除已存在的历史记录条目；若需更正，只能新增一条更正记录。
6. 记录模板：

```markdown
## 2026-09-22 17:30

- **修改文件**
  - `src/hal/iv_serial.c`
  - `include/ivsbox/iv_serial.h`
- **问题 / 目的**：串口 raw 模式未关闭流控，导致收到数据被内核加工，帧解析错位。
- **处理方式**：在 `iv_serial_open()` 中显式关闭 CRTSCTS，设置 VMIN=0 / VTIME=0。
- **验证结果**：用 socat 虚拟串口灌入 1000 帧，解析成功率 100%。
```

---

## 规则 3：修改成功后必须提交并推送 git

**远端仓库地址**：`https://github.com/ppp1234lll/T113_S4`（分支 `main`）

具体要求：

1. **任何一次修改成功后**（代码、文档、脚本一律适用，不再限于"修复问题"），必须执行一次提交并推送到上述远端仓库备份。
2. 提交前先登记规则 2 的历史记录，**历史记录与代码在同一次提交中**；有代码改动时同时补规则 4 的代码说明。
3. 提交信息格式（遵循 Conventional Commits）：

```text
<type>(<scope>): <简短描述>

<详细说明，可省略>
```

- `type` 取值：`feat` 新功能 / `fix` 修复 / `docs` 文档 / `refactor` 重构 / `test` 测试 / `build` 构建 / `chore` 杂项
- `scope` 取值：模块名，如 `hal`、`link`、`proto`、`netmgr`、`media`、`web`
- 示例：`fix(hal): 关闭串口流控，修正帧解析错位`

4. 推送前确认远端已配置（`git remote -v` 应存在 `origin` 指向上述地址），禁止强制推送到主分支。
5. 提交范围只包含本次修改涉及的文件，**不要用 `git add -A` 或 `git add .`**，避免误提交构建产物和临时文件。
6. 若本次修改涉及密钥、口令、设备序列号等敏感内容，**禁止提交**，必须先过滤。
7. 推送失败（无权限、冲突、网络异常）**必须明确告知用户失败原因和待处理项**，不得静默跳过、不得假装已推送。

---

## 规则 4：每次写代码后必须交付"代码说明"

**记录文件**：`ivsbox-v5/docs/代码说明.md`

具体要求：

1. 只要有代码产出（新增或修改 `.c` / `.h` / `.sh` / `.py` 等），写完后必须在该文件追加一节。
2. 每一节至少覆盖四点：
   - **干了什么事**：改了哪些文件、哪些函数、关键逻辑与数据流
   - **解决什么问题**：改动前的现象或缺口，改动后的行为
   - **影响范围**：会被影响的其他模块、接口、配置，以及未覆盖的风险 / TODO
   - **验证方式与结果**：编译、单测、板端实测命令与输出
3. 目的：**不看代码也能知道干了什么事、解决了什么问题**。
4. 纯文档修改（不含代码）不必写代码说明，只按规则 2 登记即可。

---

## 规则 5：优先参考天嵌官方资料

**天嵌官方资料入口**：`https://embedsky-rd.yuque.com/nqqg7d/fztngl`

遇到 T113 / 天嵌（embedsky）平台相关问题时，查找顺序固定为：

1. **先查天嵌官方语雀资料**（上述地址，含板卡手册、SDK 说明、系统配置、外设调试等）。
2. 其次查天嵌 SDK 源码、板端实际文件与官方示例（以板端实际为准）。
3. 官方资料确实不覆盖时，才寻找其他公开资料；此时必须**标注来源**，并说明为何官方资料不适用。

严禁凭猜测编造寄存器、引脚、分区、设备节点、工具链等平台事实；查不到就写"待确认"。

---

## 规则执行检查表

每次完成任务后，逐项自查：

- [ ] 功能是否已回写到对应 `.md` 文档的"实现记录"章节（规则 1）
- [ ] 是否已在 `docs/修改记录.md` 登记本次修改的文件、问题、时间（规则 2）
- [ ] 是否已提交并推送到 `https://github.com/ppp1234lll/T113_S4`（规则 3）
- [ ] 提交信息是否符合 Conventional Commits 格式
- [ ] 提交的文件是否只包含本次修改，未夹带构建产物和敏感信息
- [ ] 有代码改动时，是否已在 `docs/代码说明.md` 写明"干了什么事、解决什么问题、影响范围、验证结果"（规则 4）
- [ ] 涉及 T113 / 天嵌平台事实时，是否已先查天嵌官方语雀资料并标注出处（规则 5）

---

## 关联文档

| 文档 | 路径 | 说明 |
|---|---|---|
| 系统架构 | `T113运维终端-系统架构设计-v5-整合版.md` | 架构基线 |
| 缺口与风险清单 | `T113运维终端-v5架构缺口与风险清单.md` | G1~G19 |
| 功能开发列表 | `ivsbox-v5/docs/IVSBox-功能开发列表.md` | S01~S20 开发步骤 |
| 修改记录 | `ivsbox-v5/docs/修改记录.md` | 规则 2 的权威记录文件（即 `xiugaijilu.md`） |
| 代码说明 | `ivsbox-v5/docs/代码说明.md` | 规则 4 的权威记录文件 |
