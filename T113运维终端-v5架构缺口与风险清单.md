# T113 运维终端 v5 架构 缺口与风险清单

> 评审对象：`F:\debug\T113_TCP\T113运维终端-系统架构设计-v5-整合版.md`（723 行 / 19 章）
> 对比基线：`T113运维终端-功能架构设计-v4-Linux原生版.md`（952 行 / 80KB）
> 评审日期：2026-09-22
> 信息来源：外部评审（5 条）+ 本次复核补充（新增 14 条）
> 用途：作为 v5 定稿前的问题登记册。**P0 项未关闭前不建议冻结 §18 并进入 M1 编码。**

---

## 0. 总体结论

**架构骨架可用，细节完备度不足。** v5 相对 v4 是"提纯"而非"整合"：新增了工程治理章节（H2 标题 14 → 20 个），
同时删掉了功能实现级细节（文档量 952 → 723 行）。

| 维度 | 评分（0~5） | 说明 |
|---|---|---|
| 进程与并发模型 | 5 | 单 Reactor + 2 有界 worker + 健康线程，业务路径无锁 |
| 状态与数据所有权 | 5 | 一库一写者，Web 不直连业务库 |
| 可测试性 | 5 | fuzz + 故障注入 + 72h 长稳 + 10 条验收场景 |
| 网络与双 WAN | 4 | 已修正 v4 的"只改路由不重连"错误；缺计费保护策略 |
| 进程通信 IPC | 4 | SEQPACKET + 版本化头 + SO_PEERCRED；缺大数据路径 |
| OTA 与回滚 | 4 | 应用/系统双路径已拆开；缺提权触发机制 |
| 安全边界 | 4 | 强制签名 + 服务白名单；提权路径有缺口 |
| 媒体与存储 | 3 | 上限全是符号无数字 |
| **资源预算与可落地性** | **2** | **本清单最大短板** |

**v5 已修正 v4 的两处硬错（值得保留）**
1. 双 WAN：v4 §5.3"切换 = netlink 改路由（毫秒级），不是应用重连"是错的。v5 §7.2 明确
   "普通 TCP 不能跨接口迁移，'只改路由不重连'不作为实现方案"，切换事务含关闭旧接口 TCP + 新出口重建。
2. OTA：v4 把"应用 A/B"与"系统 U-Boot A/B"混为一谈；v5 §11.2/§11.3 拆成两条独立路径。

---

## 1. 优先级总表

| ID | 缺口 | 严重度 | 来源 | 主要影响 | 关闭阶段 |
|---|---|---|---|---|---|
| **G1** | 进程级资源预算缺失（只有上限符号，无数字） | P0 | 外部评审 ④ | 硬件选型误判、参数拍脑袋 | M0 / §18 |
| **G2** | 喂狗判据与慢任务混淆，弱网易误复位 | P0 | 本次 | 现场"设备无故重启" | M1 |
| **G3** | 非特权进程拉起 root 升级器的提权路径未定义 | P0 | 本次 | 安全关键路径设计缺口 | M0 / §18 |
| **G4** | Web 下载媒体文件的跨进程路径自相矛盾 | P0 | 本次 | 功能无法实现或破坏所有权声明 | M1 |
| **G5** | 启动顺序与就绪分级未定义 | P0 | 外部评审 ③ | 冷启动打摆子、启动期被狗复位 | M1 |
| **G6** | 慢任务池容量无量化验证（固定 2 worker） | P0 | 外部评审 ② | 探活样本饿死 → 切换/自愈判据失真 | M4 前 |
| **G7** | 模块边界无强制手段 | P1 | 外部评审 ① | 纪律靠自觉，几个版本后腐化 | M1 |
| **G8** | procd respawn 无熔断 | P1 | 本次 | 崩溃循环被掩盖成"一直在重启" | M1 |
| **G9** | 时间跳变对媒体/审计/自愈/平台的连带影响 | P1 | 外部评审 ⑤ | 录像索引错乱、TLS 连不上、熔断失效 | M3 |
| **G10** | 现场诊断能力缺失（core dump + 远程取证通道） | P1 | 外部评审 ⑤ | 现场故障无法取证 | M5 |
| **G11** | 五类版本缺兼容矩阵 | P1 | 本次 | 部分升级导致不可预期行为 | M1 |
| **G12** | 错误码字典 / IPC 方法 ID 未进 §18 冻结项 | P1 | 本次 | 接口漂移 | M1 |
| **G13** | §2 功能表缺阶段映射，"三进程协作"无主责 | P2 | 本次 | 追踪性弱 | M0 |
| **G14** | 自愈动作依赖 UART，断链时规则空转 | P2 | 本次 | 无意义动作失败审计刷屏 | M4 |
| **G15** | 平台 TLS 与"服务端无感知"冲突未定位 | P2 | 本次 | 安全与兼容两条约束在实施期打架 | M0 |
| **G16** | 缺省"无线优先"会烧 4G 流量（`revert`/`billing_guard` 丢失） | P2 | 本次 | 真金白银的流量费 | M3 |
| **G17** | media 是否读 `config/media.json` 未说明 | P2 | 本次 | 配置重载路径不清 | M1 |
| **G18** | TLS 握手 / 配置 `fsync` 是否在 Reactor 回调内未排除 | P2 | 本次 | 主循环阻塞 → 误喂狗 | M1 |
| **G19** | 隧道帧结构等实现级参数在 v5 丢失 | P2 | 本次 | M3 需回头重做决策 | M3 前 |

**严重度定义**
- **P0**：编码前必须冻结或解决，否则返工或产生现场故障。
- **P1**：进入对应阶段前必须解决。
- **P2**：随文档迭代补齐，不阻塞主线。

---

## 2. P0 缺口详情

### G1　进程级资源预算缺失

**现状**
- §4.1 仅有"限制 CPU/内存"一句；所有上限（队列、连接、码率、`media.max_channels`）都是符号，无数字。
- §18 第 9 条已把"Flash/内存/CPU 预算"列为冻结项 —— **有位置，没内容**。
- v4 至少有 cgroup 配额行（§5.4）与依赖体积/可否省略表（附录 A），**v5 全部删除**。

**风险后果**：T113-S3 为 2×Cortex-A7 + 片内 128MB DDR3，需同时容纳 `ivsboxd`（TLS + SQLite + 状态镜像）、
HTTP 服务、FFmpeg remux 1~3 路写 SD、logd、procd、4G 拨号栈。**没有量级锚点，参数会被拍脑袋**，
且到 M6 才会第一次真正讨论，届时改动成本最高。

**建议措施**
1. 在 §18 增补"资源预算"为第 10 类冻结项，并给出附录级估算表（见本文附录 A）作为起步假设。
2. 把 cgroup 从 §14 的"待确认组件"提回架构正文 —— 它是 media 隔离的**实现手段**，不是可选依赖。
3. `media.max_channels` 的缺省值由预算表倒推（建议缺省 2，硬上限 3~4），不得独立配置。
4. 同时核算**文件描述符**上限：探活并发 × socket + ONVIF/SNMP + IPC + 录像会话，
   与系统缺省 `ulimit -n`（常为 1024）比对。

**验收方式**：M1 阶段在真实 BSP 上跑空载 + 3 路录像，输出 `VmRSS` / `VmPeak` / fd 计数实测表。

---

### G2　喂狗判据与慢任务混淆

**现状**
- §4.2：慢任务池处理 SNMP、ONVIF、RTSP 能力探测、DNS。
- §12.3："主循环或**关键模块**超出预算时记录最小故障信息并停止喂狗。"

**风险后果**：4G 弱网下 SNMP 超时、DNS 挂起、ONVIF SOAP 无响应都是**正常现象**。
若健康判据包含"慢任务进度"，一次 30s 的 SNMP 阻塞即触发喂狗超时 → 整机复位 →
现场表现为"设备无故重启"，且日志里看不到原因（复位时来不及落盘）。

**建议措施**
1. 明确写死：**健康判据只看主事件循环的原子进度号 + 关键链路时间戳**；
   慢任务只做"超时计数 + 告警"，**不参与喂狗**。
2. 给出时间关系式：`watchdog timeout > procd 重启间隔 + 进程启动到首次喂狗的时间`，
   否则 `ivsboxd` 正常崩溃重启也会被硬件狗咬。
3. 停止喂狗前的最小故障信息必须先 `fsync` 落盘（否则复位即失证据）。

**验收方式**：故障注入 —— 人为把 SNMP 目标指向黑洞地址（30s 超时），观察设备不复位且产生告警。

---

### G3　非特权进程拉起 root 升级器的提权路径未定义

**现状**
- §4.1 / §13.1：`ivsbox-updater` 为 root、按需运行；`ivsbox-provision` 仅工厂模式可运行。
- 全文**未说明由谁、通过什么机制**启动它们。`ivsboxd` 是普通用户 + 少量 capabilities。
- §11.2 的"连续启动失败计数"存储位置与防篡改也未写。

**风险后果**：这是全文唯一一处权限设计缺口，且正在安全关键路径上。实现者若自行发明机制
（如给 `ivsboxd` 加 setuid 辅助程序、或让 `ivsboxd` 直接持 root），会把整个特权模型推翻。

**建议措施**
1. 固定触发方式（推荐：`procd`/`ubus` 受限服务，或预约定路径的触发文件）。
2. 硬约束：**不可传入路径/命令参数**（不接受任何来自网络或 Web 的目标路径）；
   `ivsbox-updater` 必须**自行重新验签**，不信任调用方的校验结果。
3. 回滚计数落在受保护存储（如工厂分区或带签名的状态文件），并说明防篡改手段。
4. 量产后关闭 `ivsbox-provision` 入口（§13.1 有方向，需给出具体机制）。

---

### G4　Web 下载媒体文件的跨进程路径自相矛盾

**现状**
- §9.1 Web 页面含"媒体：图片、录像、下载"。
- §10.1 / §4.4：媒体文件归 `ivsbox-media`；`ivsbox-web` 是"无特权用户、不访问媒体目录"。
- §5.1 仅含糊写了"超限数据通过文件句柄、任务 ID 或分片协议传递"。

**风险后果**：需求与约束直接冲突，实现者必须自行选择，选择结果会影响权限模型。

**建议措施**（三选一，写进架构）
| 方案 | 说明 | 代价 |
|---|---|---|
| A（推荐） | `ivsbox-media` 经 `SCM_RIGHTS` 传 fd 给 Web | 权限不扩散，天然支持 Range 定位；实现稍复杂 |
| B | 给 Web 一个只读白名单媒体目录 | 简单，但破坏"媒体文件唯一归 media"的声明 |
| C | Web 反向代理到 media 自身的 `127.0.0.1` HTTP 服务 | 实现最省；media 需开监听口，须限本机 + 鉴权 |

同时明确"未确认数据补传队列"的文件读写权限归属。

---

### G5　启动顺序与就绪分级未定义

**现状**
- §4.1 仅"所有常驻进程由 procd 管理"；§5.1 仅"服务重启后客户端重新连接并重新订阅事件"。
- 未定义初始化分级、启动时间预算、主控未就绪时 media/web 的重连退避。

**风险后果**
1. `procd` 并发拉起三个常驻进程 → media/web 在 `ivsboxd` socket 尚不存在时连接失败 →
   若无可退避重连，即 respawn 循环（冷启动打摆子）。
2. 与 G2 联动：首次喂狗时机未定义 → 启动期可能被狗复位。
3. §11.2 的 OTA 确认窗口（60s）若小于弱网下 L4 达成时间 → **误回滚**。

**建议措施**：补一节"启动顺序与就绪分级"，至少按下表定义，并把两条时间约束写成硬约束。

| 级别 | 内容 | 目标时间 | 依赖 |
|---|---|---|---|
| L0 | 核心就绪：IPC socket bind、UART 打开 | ~2s | 本地 |
| L1 | 门禁/开锁可执行 → **首次喂狗** | ~4s | UART |
| L2 | Web 本地运维可用（可登录看状态） | ~8s | IPC |
| L3 | 双 WAN 选路 + 探活启动 | ~12s | 内核/模组 |
| L4 | 平台连接 + 会话恢复 + 补传 | ~20s | 外部网络 |
| L5 | 设备面首轮探测（摄像机/SNMP/ONVIF） | 可延迟 | 外部设备 |

**两条硬约束**
- `watchdog timeout > L1 达成时间`，首次喂狗前不得开狗；
- `OTA 确认窗口 > L4 最坏达成时间`（弱网下可达数十秒）。

另需明确：L0 之前 media/web 的重连退避参数（建议指数退避 + 上限），
以及"平台不可达时以 L5 之外的能力降级运行"的语义。

---

### G6　慢任务池容量无量化验证

**现状**
- §4.2："固定两个 worker……队列长度固定，满时返回忙状态。"
- §16.1 集成测试无"多摄像机并发探测"场景。

**风险后果（比"排队超时"更严重）**：按 M4 现实负载估算，32 台摄像机 ×
（SNMP ~2s + ONVIF ~1s + RTSP 探活 ~2s）≈ **160 秒工作量/轮**，2 个 worker = 80 秒墙钟。
若巡检周期为 30 秒，则**队列永久积压，worker 100% 时间在超时重试**。
此时真正的问题不是"某次探测慢"，而是**探活样本被饿死** —— 双 WAN 的 `fail_n`/`ok_n` 滞回判据、
自愈引擎的"连续失败 N 次"、上游同源判断全部建立在这套样本上，样本陈旧会导致
**切换与重启的决策依据错误**。

**建议措施**
1. §16.1 增补量化场景："N 台摄像机（按现场实际路数）并发 SNMP + ONVIF + RTSP 探测"，
   给出 N、巡检周期、最坏单次耗时的预算表。
2. 队列满改为**丢最旧 + 计数告警**，而不是只返回忙 —— 否则积压永远不可见。
3. 对"worker 数可配"持保留意见：worker 数是并发上界，同时也是内存消耗上界，
   开放配置后现场必然被调大。建议改为**分池 + 按轮预算限流**：
   控制类快任务（ICMP 等）留在 Reactor，设备探测类单独成池，配合 token bucket 限流，
   宁可拉长周期也不允许积压。

---

## 3. P1 缺口详情

### G7　模块边界无强制手段

**现状**：§6 给出 5 条依赖规则（全部为散文），§15 仅提到"Host：……和静态分析"。
**并且这是相对 v3 的回退** —— v3 工程结构含 `scripts/ci/check_layering.sh`，v5 §15 只留 `scripts/init.d/`。

**判断修正**：**"`ivsboxd` 是大单体"本身不是风险**。128MB 内存 + 2×A7 的板子上，
继续拆进程只会让每个进程各带一份 libc/mbedTLS，并把业务状态切成跨进程事务。
真正的风险是**边界靠自觉**。因此落点应是编译期强制，而非"拆进程"。

**建议措施**
1. `include/ivsbox/` 只放公共头；`src/modules/<x>/` 的私有头不进公共 include 路径（目录即边界）。
2. `core` / `hal` / 各 module 分别编成静态库，用 CMake `target_link_libraries` 把依赖方向写死
   —— **越界直接链接失败**，比扫描 include 硬得多。
3. `check_layering.sh` 作为兜底重新加回并进 CI。

---

### G8　procd respawn 无熔断

**现状**：§12.3 表格仅"服务级：procd `respawn`"。§4.1 说"主控异常由 procd 先拉起，
无法恢复时由硬件看门狗复位整机"，但未定义"无法恢复"的判据。

**风险后果**：进程崩溃循环会被 respawn 掩盖成"设备一直在重启"，问题在日志里表现为噪音而非故障。

**建议措施**：设置 respawn 次数上限；超限进入 failsafe 并**停止喂狗**（让硬件兜底），
同时产生最高级别告警。明确"无法恢复"= 单位时间内 respawn 次数超阈值。

---

### G9　时间跳变对媒体/审计/自愈/平台的连带影响

**现状**：§7.7 只写了"小偏差渐进校正，大偏差在记录审计后设置，避免日志时间反复跳变"。

**风险后果**（外部评审提了命名与索引，实际影响面更大）
1. **录像切片**：§8.2 按 1~5 分钟切片，若文件名含本地时间，时钟回跳会造成文件名乱序甚至覆盖。
2. **media.db 索引**：§10.3 未规定 `ts` 字段时区，回跳会造成区间查询错乱。
3. **TLS 时间死锁（最严重）**：设备上电时 RTC 未同步 → 时间回到 1970 或默认值 →
   证书有效期校验失败（"certificate not yet valid"）→ **平台连不上** → NTP/平台授时也拿不到 →
   永久无法自愈。这是一个鸡生蛋的死锁，必须在设计上打破。
4. **自愈熔断失效**：§7.6 的"冷却、每小时/每天上限、时间窗口"全部依赖墙钟。
   时钟跳变会让频率限制算错 —— 可能绕过熔断造成重启风暴，也可能误熔断。
5. 门禁开锁的"有效期"、隧道 token 的 `ttl_s`、审计时间戳同样依赖时钟。

**建议措施**
1. **分离时钟**：所有时长、窗口、超时、退避一律用 `CLOCK_MONOTONIC`；
   墙钟只用于"给人看的时间戳"和证书校验。
2. 墙钟增加"**可信标志**"：未同步前，媒体索引存"UTC + 单调计数"双字段，
   切片文件名用单调序号而非本地时间。
3. 时间不可信时放宽 TLS 证书时间校验（仅限时间维度，**不得**放宽链与主机名校验），
   或预置平台证书的时间容错窗口，明确记录该降级的审计。
4. 大偏差跳变前先归档当前媒体索引批次，避免跨批次查询错乱。

---

### G10　现场诊断能力缺失

**现状**
1. **core dump：v4 有（"core dump 留证"），v5 §12.1 全部删除**，且 §13.1 权限表未涉及。
2. **远程取证通道**：§9.4 白名单只列了 `web`、`camera:<id>`、`media:<id>`，
   **没有日志/状态取证服务名** —— 断网录像出问题时无法远程取证。

**建议措施**
1. 补 core dump 策略：是否启用、存放路径（建议 `/data/ivsbox/core/`）、单个大小上限、
   总配额与滚动策略、是否随故障日志上报。
2. §9.4 白名单增补只读诊断服务名（如 `log` / `diag`），并明确：
   **只读、限速、有大小上限、不提供 shell、全部操作记审计**。
3. 明确"media 崩溃/录像异常"场景的取证步骤（这是最容易现场扯皮的一类故障）。

---

### G11　五类版本缺兼容矩阵

**现状**：§5.1 定义了 IPC 协议版本，§10.2 定义了配置 schema 版本，但无统一视图。
§4.2 未定义 IPC 版本不匹配时的行为（拒绝并告警 vs 静默降级）。

**建议措施**：补一张兼容矩阵，覆盖 **IPC / DB schema / 配置 schema / 平台协议 / 采集板协议** 五类版本，
并明确三条规则：
1. 三个常驻进程必须同 release、整体重启；
2. IPC 版本不匹配 → **拒绝连接并告警**，不得静默降级；
3. DB / 配置 schema 升级必须向前兼容一个版本（可读旧、写新）。

---

### G12　错误码字典 / IPC 方法 ID 未进 §18 冻结项

**现状**：§9.2 说"API 返回稳定错误码"，§7.1 提到"失败细分码"，但全文无错误码表；
§5.2 只列了若干典型方法名，无完整方法 ID 表。§18 的 9 类冻结项未包含这两项。

**建议措施**：把"错误码字典（探活失败细分码 + 平台/业务错误码 + IPC 错误码）"
与"IPC 方法 ID 表"补入 §18 冻结项，与平台协议、UART 协议同级别管理。

---

## 4. P2 缺口详情

| ID | 现状 | 建议 |
|---|---|---|
| **G13** | §2 功能表只有"核心/条件能力/硬件待确认/平台待确认"，无 M0~M8 映射；第 18 项"归属：三个常驻进程协作"等于无归属 | 增加阶段列；每项指定**主责进程**，协作方列为消费方 |
| **G14** | 自愈动作（断电/上电）全部由采集板执行（§7.6 + §2 第 7 项），但未定义 UART 断链时的行为 | 增加显式分支：链路不可用 → 抑制动作 + 告警 + **不计入熔断**，否则链路故障期会刷出大量无意义的动作失败审计 |
| **G15** | §13.2"平台连接使用 TLS……若现网暂不支持，形成明确整改项"，与 §1.3"私有协议默认字节级兼容"存在张力 | 降级为与 §2 第 22 项相同的"平台待确认"标记，M0 明确"若平台不支持 TLS，谁让步" |
| **G16** | §7.2 缺省"无线优先、恢复后切回无线"；v4 的 `wan.revert = auto\|no_revert` 与 `billing_guard`（流量阈值内不回切）**已删除** | 补回两个配置项 —— 缺省行为会消耗 4G 流量费，属真金白银的需求 |
| **G17** | §4.4 只说了 Web 不写配置，未说明 media 是否读 `config/media.json` | 明确：media **只读**，重载由 ivsboxd 经 IPC 通知 |
| **G18** | §4.2 排除的是"DNS、同步 SNMP、同步 ONVIF、长时间文件 IO"；**TLS 握手与配置 `fsync + rename` 未明确排除** | 明确 TLS 握手走非阻塞状态机或交给 worker；配置写入（含目录 fsync）在 worker 完成，完成后事件回投 Reactor 提交状态 |
| **G19** | 隧道帧结构等实现级参数在 v5 丢失 | 见附录 B，按需从 v4 取回 |

---

## 附录 A　资源预算估算草表（起步假设，必须 M0 实测替换）

> 说明：下表为**量级估算**，用于给参数取值提供锚点，**不得作为结论使用**。
> 实测方法：空载与 3 路录像两种工况下采集 `/proc/<pid>/status` 的 `VmRSS`/`VmPeak`、
> `ls /proc/<pid>/fd | wc -l`、`cat /proc/meminfo`。

| 项 | 起步假设 | 备注 |
|---|---|---|
| 内核 + 系统保留 + 页缓存 | 25~35 MB | 页缓存可回收，但写 SD 时会增长 |
| logd + procd + netifd + udhcpc + nftables | 4~6 MB | 系统组件 |
| `ivsboxd` | 6~10 MB | SQLite cache 建议 ≤2MB、mbedTLS ~0.5MB、状态镜像 |
| `ivsbox-web` | 4~8 MB | 代理缓冲 + 静态资源 + TLS |
| `ivsbox-media`（空闲） | 3~5 MB | |
| `ivsbox-media`（每路 RTSP remux） | 8~15 MB | 含 4~8MB 网络/IO 缓冲；**不转码** |
| 4G 拨号栈 | 3~6 MB | QMI/ppp + 热插拔脚本 |
| **合计（3 路录像）** | **70~100 MB** | 128MB 下余量约 25~55MB，**无压力测试余量** |

**结论**：`media.max_channels` 缺省值应由本表倒推（建议缺省 2，硬上限 3~4）。
另需单独核算文件描述符总额，并与系统缺省 `ulimit -n` 比对。

---

## 附录 B　v5 相对 v4 丢失的实现级参数（建议按需搬回对应小节）

搬回成本极低，收益是验收可直接判定。

| 类别 | v4 中的具体内容 | v5 现状 |
|---|---|---|
| 隧道 | `TUN_OPEN` / `TUN_OPEN_ACK` / `TUN_DATA`(≤8KB) / `TUN_CLOSE`；token `ttl_s=300`；流分 CONTROL / BULK 两级（有线 512kbps、4G 256kbps） | 只剩"受控多路复用" |
| 平台重连 | 指数退避序列 `1,2,4,8,16,30s`（上限 30s）+ 抖动 | 无 |
| SNMP | OID 模板匹配优先级（精确 > 通配 > generic）；`scale` / `offset` 换算；"立即测试 + 热重载" | 只剩"品牌/型号模板" |
| 日志 | 运行日志保留 14 天 / 故障日志 90 天；`/data/log/run|fault` 路径 | 只剩"故障日志保留更长" |
| 双 WAN | `wan.revert = auto\|no_revert`；`billing_guard` | 删除（见 G16） |
| 配置 | "U 盘导入"作为第三种配置来源 | 删除 |
| 音频 | "静默时段" | 删除 |
| 附录 | 附录 A 依赖清单（含体积/可否省略）、附录 B 设计取舍速查 | 无附录 |
| 媒体 | cgroup 限 CPU/内存 | 仅剩"限制 CPU/内存"文字 |
| 诊断 | core dump 留证 | 删除（见 G10） |

---

## 附录 C　评审方法与数据

- **文档量对比**：v4 = 952 行 / 79.7KB；v5 = 723 行 / 36.2KB（体积 -55%）。
- **结构对比**：v5 的 H2 标题 20 个 vs v4 的 14 个 —— 说明 v5 是"**新增治理章节 + 删减功能细节**"，
  而非标题字面意义上的"整合"。这一点决定了不能假设"v4 的信息都在 v5 里"。
- **核查手段**：逐条把 v5 的约束（§4.1、§4.2、§5.1、§9.4、§10.1、§12.3、§13.1、§15、§18）
  与 v4 对应章节（§2.1、§4.6、§4.19、§4.22、§5.4、§5.8、附录 A/B）对读，
  凡 v5 出现"上限/限制/更长/按需"等模糊限定词处，回查 v4 是否有具体数值。

---

## 附录 D　建议的关闭顺序

1. **M0 冻结项**：G1（预算表）、G3（提权机制）、G15（TLS 定位）→ 更新 §18。
2. **M1 编码前**：G2（喂狗判据）、G4（媒体文件路径）、G5（启动分级）、G7（编译期边界）、G8（respawn 熔断）、G11、G12、G17、G18。
3. **M3 前**：G9（时钟分离）、G16（计费保护）。
4. **M4 前**：G6（并发探测量化 + 分池限流）、G14（UART 断链抑制）。
5. **M5 前**：G10（诊断能力）。
6. **随迭代**：G13、G19。
