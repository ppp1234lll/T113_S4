# T113 运维终端系统架构设计

> 文档状态：架构基线（重制版，按用户设计理念持续迭代）  
> 目标平台：全志 T113 / Tina Linux（OpenWrt 体系）  
> 主体语言：C11  
> 构建系统：GNU Make（不使用 CMake）  
> 首版日期：2026-09-22　最近修订：2026-09-24 14:26  
> 前身：`T113运维终端-系统架构设计-v5-整合版.md`（保留为备份，不再维护）

## 0. 文档目的

本文给出 T113 运维终端的软件系统架构，用于指导详细设计、任务拆分、接口冻结、编码和验收。

整合原则如下：

1. 保留 v4 对全部功能、Web API、统一探活、自愈策略、出厂工具及测试体系的完整描述。
2. 采用控制面、媒体面、管理面分进程的故障隔离方式。
3. 主控内部采用单 Reactor + 有界慢任务池，减少线程和跨线程状态同步。
4. 明确 IPC 消息边界、数据库所有权和进程权限，禁止隐式共享状态。
5. 修正双 WAN 切换、OTA、UART 兼容以及远程访问通道中的实现矛盾。
6. 已有协议保持兼容；所有新增协议能力必须经过版本协商，不能假定平台或采集板自动兼容。
7. 构建体系只使用 GNU Make 手写 Makefile，不引入任何额外的构建生成器。

## 0.1 实现记录

> 本文档的架构修订项记录（规则 1），**倒序**，最新在上。

| # | 修订项 | 状态 | 完成时间 | 说明 |
|---:|---|---|---|---|
| 2 | 删除 §17 实施路线图中的 M0 接口冻结阶段 | 已实现 | 2026-09-24 14:40 | 用户明确 M0 不必要；§17 表格移除 M0 行，后续阶段编号不变 |
| 1 | 构建系统改为纯 Makefile，去除 CMake | 已实现 | 2026-09-24 14:26 | §0 增整合原则 7；§1.3 增构建约束；§14 增"构建系统"选型行；§15 整章重写为"工程结构与构建体系"；§17 M1 退出条件同步 |

## 1. 系统边界与约束

### 1.1 T113 职责

- 4G/有线网络管理和平台通信。
- GPS 定位读取与上报。
- 摄像机、交换机发现、探测、SNMP 和档案管理。
- Web 运维、日志、升级和出厂配置。
- RTSP 录像、抓图、视频读取和音频广播。
- 业务状态维护、自愈策略和硬件看门狗门控。

### 1.2 采集板职责

- 计量、温湿度、倾斜、ADC、RS485 等采集类 IO。
- 电磁锁、继电器、摄像机和网络设备电源通道的最终执行。
- 门磁及其他实时 IO 事件上送。
- 对 UART 控制命令返回明确执行结果。

### 1.3 必须遵守的约束

- 平台现有私有协议和采集板现有 UART 协议默认字节级兼容。
- 不允许在未升级对端的情况下擅自增加 UART 字段、修改 CRC 或改变字节序。
- 控制业务不能因 FFmpeg、Web 或摄像机异常而中断。
- 密钥、口令、升级包和远程开锁属于安全边界，不能降级为仅 CRC 校验。
- 所有队列、线程、连接、录像通道和磁盘占用必须存在明确上限。
- 构建只使用 GNU Make；不使用 CMake、Meson、Autotools、Ninja 或任何构建生成器，不要求编译机安装 Make 之外的构建软件。

## 2. 功能范围与归属

| # | 功能 | 子系统 | 归属进程 | 阶段 |
|---:|---|---|---|---|
| 1 | 主网通断、延迟、丢包检测 | 统一探活、网络管理 | `ivsboxd` | 核心 |
| 2 | 摄像机 ICMP/TCP/RTSP 检测 | 统一探活、设备管理 | `ivsboxd` | 核心 |
| 3 | UART 与采集板通信 | 板间链路 | `ivsboxd` | 核心 |
| 4 | 无线与平台对接 | 平台协议 | `ivsboxd` | 核心 |
| 5 | 无线/有线/自动切换 | 多 WAN 管理 | `ivsboxd` + 内核路由 | 核心 |
| 6 | 远程升级 | OTA | `ivsboxd` + `ivsbox-updater` | 核心 |
| 7 | 外部设备自动重启 | 自愈策略 | `ivsboxd` + 采集板 | 核心 |
| 8 | Web 图形管理 | Web 管理 | `ivsbox-web` | 核心 |
| 9 | GPS 经纬度上报 | GPS 管理 | `ivsboxd` | 核心 |
| 10 | 设备 ID、MAC 写入 | 出厂工装 | `ivsbox-provision` | 核心 |
| 11 | 电磁锁控制 | 门禁管理 | `ivsboxd` + 采集板 | 核心 |
| 12 | 摄像机 CPU/内存/码流 | SNMP | `ivsboxd` | 核心 |
| 13 | SNMP OID 配置 | 配置、SNMP | `ivsboxd` + `ivsbox-web` | 核心 |
| 14 | 交换机信息读取 | SNMP | `ivsboxd` | 核心 |
| 15 | 一机一档 | ONVIF、档案库 | `ivsboxd` | 核心 |
| 16 | 开门抓图 | 门禁、媒体 | `ivsboxd` + `ivsbox-media` | 核心 |
| 17 | 断网视频存储 | 网络、媒体 | `ivsboxd` + `ivsbox-media` | 核心 |
| 18 | 远程视频读取 | 媒体、Web/平台协议 | 三个常驻进程协作 | 核心 |
| 19 | 远程摄像机 Web | Web 代理、远程通道 | `ivsbox-web` + `ivsboxd` | 条件能力 |
| 20 | 运行与故障日志 | 日志、审计 | 全部进程 | 核心 |
| 21 | 音频广播 | 音频媒体 | `ivsbox-media` | 硬件待确认 |
| 22 | 远程访问通道 | 安全隧道 | `ivsboxd` + 平台 | 平台待确认 |

“条件能力”只有在前置接口确认并通过安全评审后进入交付范围，不得仅凭架构文档认定为现网可用。

## 3. 总体架构

```text
                              云平台 / 运维客户端
                 控制协议、遥测、升级、媒体上传、授权远程访问
                                      │
                              4G / 有线网络
                                      │
┌──────────────────────────────── T113 Linux ────────────────────────────────┐
│                                                                            │
│  ┌────────────────────────── ivsboxd ───────────────────────────────────┐  │
│  │ 平台协议 │ UART │ GPS │ 多 WAN │ 探活 │ 设备档案 │ SNMP │ 自愈策略 │  │
│  │ 状态中心 │ 配置 │ 审计 │ OTA协调 │ 健康门控 │ 可选远程通道          │  │
│  └───────────────────┬────────────────────────┬─────────────────────────┘  │
│                      │ UDS/SEQPACKET          │ UDS/SEQPACKET              │
│  ┌───────────────────▼───────────────┐  ┌─────▼────────────────────────┐  │
│  │ ivsbox-media                      │  │ ivsbox-web                   │  │
│  │ RTSP录像、抓图、媒体索引          │  │ REST、静态页面、视频下载     │  │
│  │ 存储水位、音频播放                │  │ 摄像机Web受控代理、鉴权       │  │
│  └───────────────────┬───────────────┘  └──────────────────────────────┘  │
│                      │                                                     │
│  ivsbox-updater（按需、特权）  ivsbox-provision（出厂、按需）              │
│  procd │ logd │ netifd/udhcpc │ /dev/watchdog │ SQLite │ SD卡             │
└──────────────────┬─────────────────────────────────────┬───────────────────┘
                   │ UART                                │ Ethernet
           ┌───────▼──────────┐              ┌──────────▼────────────┐
           │ MCU 采集板       │              │ 摄像机、交换机        │
           │ 门锁/继电器/IO   │              │ RTSP/ONVIF/SNMP/HTTP  │
           └──────────────────┘              └───────────────────────┘
```

### 3.1 核心决策

| 决策 | 结论 |
|---|---|
| 进程划分 | 主控、媒体、Web 三个常驻业务进程，升级和出厂工具按需运行 |
| 状态模型 | `ivsboxd` 是控制与设备状态的唯一权威来源 |
| 主控并发 | 一个 `epoll` Reactor、固定大小慢任务池、独立健康线程 |
| 进程通信 | Unix Domain `SOCK_SEQPACKET`，版本化消息头，JSON 或定长二进制载荷 |
| 数据所有权 | 控制库归主控、媒体库归媒体；Web 不直接打开业务数据库 |
| 网络切换 | 内核路由切换后，绑定旧接口的 TCP 必须关闭并从新出口重建 |
| OTA | 应用双版本与系统 A/B 分开设计，所有升级包强制数字签名 |
| UART | 兼容现有帧；可靠性增强取决于采集板能力协商 |
| 远程访问 | 需要平台能力和安全协议支持，不与“现网无感知”同时承诺 |
| 协议实现 | 优先成熟库；仅私有平台协议、板间协议和业务状态机自研 |
| 构建系统 | 纯 GNU Make 手写 Makefile，单一顶层入口，不使用 CMake |

## 4. 进程、线程与所有权

### 4.1 进程清单

| 进程 | 职责 | 故障影响 | 运行权限 |
|---|---|---|---|
| `ivsboxd` | 控制、通信、状态、配置、设备管理、自愈和健康门控 | 核心业务中断 | 普通用户 + 必需 capabilities |
| `ivsbox-media` | RTSP、抓图、录像、媒体索引、音频 | 媒体能力中断 | 普通用户、限制 CPU/内存 |
| `ivsbox-web` | Web API、静态页面、下载、受控反向代理 | 管理界面中断 | 无特权用户 |
| `ivsbox-updater` | 验签、安装、切换、回滚 | 仅升级阶段 | root，按需运行 |
| `ivsbox-provision` | 出厂写设备身份及 MAC | 仅生产阶段 | 受控工装权限 |

所有常驻进程由 procd 管理。媒体和 Web 崩溃只重启自身；主控异常由 procd 先拉起，无法恢复时由硬件看门狗复位整机。

### 4.2 `ivsboxd` 并发模型

```text
 UART ───────┐
 平台TCP ────┤
 rtnetlink ──┤
 GPS ────────┼──> main_reactor(epoll) ──> 唯一状态写入者
 timerfd ────┤                │
 signalfd ───┤                ├──> 有界慢任务队列 ──> 2个worker
 IPC socket ─┘                │                    └─> eventfd回传结果
                              └──> 原子进度号 ──> health_worker
```

#### 主 Reactor

- 统一处理 UART、平台 Socket、Netlink、GPS fd、IPC、`timerfd`、`signalfd` 和 `eventfd`。
- 负责业务状态唯一写入、状态机推进、命令分流和异步任务编排。
- 单次回调不得进行 DNS、同步 SNMP、同步 ONVIF、长时间文件 IO 等不可控阻塞。
- 每轮事件循环递增原子进度号，供健康线程判断主循环是否真实前进。

#### 慢任务池

- 固定两个 worker，处理 SNMP、ONVIF、RTSP 能力探测、DNS 和其他有明确超时的任务。
- 队列长度固定，满时返回忙状态，不允许无限排队或临时创建线程。
- worker 不修改全局业务状态，只通过完成事件返回结果。
- 每个任务必须具备截止时间、取消标志和结果长度上限。

#### 健康线程

- 独立于 Reactor，每秒检查主循环进度、慢任务进度和关键链路时间戳。
- 全部健康时才喂 `/dev/watchdog`。
- 主循环或关键模块超出预算时记录最小故障信息并停止喂狗。
- 健康线程不得尝试替代主循环继续执行业务。

### 4.3 `ivsbox-media` 并发模型

- 一个控制循环处理 IPC、任务状态和进程信号。
- 每个活动 RTSP 会话使用媒体库的独立 pipeline，活动数量受 `media.max_channels` 限制。
- 抓图任务进入有界队列，默认串行或低并发执行。
- 音频使用独立高优先级任务队列；资源紧张时先暂停非关键录像。
- 存储巡检定时检查挂载状态、写入错误和水位。

### 4.4 `ivsbox-web` 并发模型

- 使用成熟 HTTP 服务实现，不自研 HTTP 解析器。
- 设置连接数、请求体、上传文件、代理响应和超时上限。
- 所有状态和控制操作通过 IPC 请求主控或媒体进程。
- 不直接写配置文件，不直接打开控制库或媒体库。

## 5. 进程间通信

### 5.1 传输与消息格式

IPC 使用 Unix Domain `SOCK_SEQPACKET`，从传输层保留消息边界。

```c
typedef struct {
    uint32_t magic;       /* 'IVSB' */
    uint16_t version;     /* IPC 协议版本 */
    uint16_t type;        /* 请求/响应/事件 */
    uint32_t flags;
    uint64_t request_id;
    uint32_t payload_len;
    uint32_t reserved;
} ivs_ipc_hdr_t;
```

- 控制和查询载荷使用 JSON，媒体内部高频状态可使用版本化定长结构。
- 单包最大长度固定，超限数据通过文件句柄、任务 ID 或分片协议传递。
- 使用 `SO_PEERCRED` 校验调用进程身份，Socket 权限为 `0660`。
- 请求必须设置超时；服务重启后，客户端重新连接并重新订阅事件。
- 长任务立即返回 `task_id`，进度和结果通过事件通知或查询接口获得。

### 5.2 IPC 服务边界

| 服务端 | 典型方法 |
|---|---|
| `ivsboxd` | `system.snapshot`、`camera.list`、`camera.probe`、`lock.open`、`config.apply`、`ota.begin` |
| `ivsbox-media` | `record.start/stop`、`snapshot.take`、`media.list`、`audio.play/stop` |
| `ivsbox-web` | 不向业务进程提供内部控制服务，只提供受鉴权的 HTTP 接口 |

## 6. 主控模块划分

```text
src/
├── app/        入口、装配、Reactor、命令路由、业务编排
├── core/       事件、队列、定时器、日志、配置、状态机、CRC
├── hal/        serial、netlink、watchdog、fs、clock、capability
└── modules/
    ├── link/       UART 板间链路
    ├── proto/      平台协议
    ├── netmgr/     网络检测和多 WAN
    ├── gps/        GPS 与时间来源
    ├── probe/      ICMP/TCP/RTSP 统一探活
    ├── device/     摄像机/交换机档案
    ├── snmp/       SNMP 采集与 OID 模板
    ├── recovery/   自愈策略
    ├── access/     门磁、门锁和审计
    ├── config/     配置事务与热更新
    ├── ota/        升级协调
    └── tunnel/     可选远程访问通道
```

依赖规则：

- `core` 不依赖 Linux HAL，可在开发机单元测试。
- 模块不能修改其他模块的私有数据。
- 业务状态只在 `app` 的 Reactor 上下文更新。
- HAL 不包含业务策略。
- 第三方协议库通过适配层接入，不把第三方类型扩散到业务接口。
- 依赖方向在顶层 `Makefile` 的链接顺序中显式声明（见 §15.5），不依赖约定。

## 7. 核心业务架构

### 7.1 统一探活引擎

统一探活引擎为主网、双 WAN、摄像机和交换机提供相同的统计与状态判定。

| 层级 | 方法 | 判定内容 |
|---|---|---|
| 链路层 | rtnetlink carrier/address/route | 网卡、地址、路由是否就绪 |
| 网络层 | ICMP | 可达性、RTT、丢包、抖动 |
| 传输层 | 非阻塞 TCP connect | 指定服务端口是否可用 |
| 应用层 | 平台心跳、RTSP OPTIONS/DESCRIBE、SNMP | 真实业务是否可用 |

探活结果统一包含状态、模式、原因码、最近 RTT、滑动平均、丢包率、连续成功/失败次数和最近成功时间。ICMP 被禁用时，TCP 或应用层成功仍应判定目标可用。

### 7.2 双 WAN 状态机

默认策略为无线优先、无线失败切有线、无线稳定恢复后切回无线。

```text
WIRELESS_UP
    │ 连续 fail_n 次失败
    ▼
WIRELESS_DEGRADED ──业务探测失败──> SWITCH_TO_WIRED ──> WIRED_UP
                                                            │
                         无线连续 ok_n 次且保持 hold_s       │
                                                            ▼
                                               SWITCH_TO_WIRELESS

任一状态下两条链路均失败 -> BOTH_DOWN
```

切换事务按以下顺序执行：

1. 冻结新的低优先级发送任务。
2. 通过 Netlink/策略路由切换默认出口。
3. 关闭绑定旧接口的 TCP 连接及其远程流。
4. 使用新接口重新解析地址并建立平台连接。
5. 完成鉴权和会话恢复。
6. 按业务优先级补传未确认数据。
7. 达到稳定期后恢复媒体上传和大文件下载。

普通 TCP 连接不能跨接口迁移，因此“只改路由不重连”不作为实现方案。

### 7.3 平台协议

- 保持现有帧格式、命令字、校验、设备类型和应答语义。
- 使用表驱动命令路由：本地执行、转发采集板、本地并转发。
- 所有请求具有关联号、超时、幂等或去重规则。
- 发送队列按“门禁/控制 > 告警 > 状态 > 媒体”分级，并设置字节和条目上限。
- 断线数据存入有容量限制的持久队列，恢复后按序补传。
- 未知命令是否转发采集板由协议白名单决定，不能无条件透传。

### 7.4 UART 板间链路

串口使用 `termios` raw 模式并纳入主 Reactor。帧解析器必须支持半包、粘包、噪声重同步、长度上限、校验失败和帧超时。

兼容策略：

1. 第一阶段严格实现现有采集板协议，不改变帧结构。
2. 如果现有协议已提供请求号和 ACK，则直接建立超时重传和去重。
3. 如果现有协议没有可靠层，先使用命令级确认和状态回读保证控制结果。
4. 只有 T113 与采集板都支持版本协商时，才能启用新增 `seq/ACK` 的协议版本。
5. 门锁和电源控制必须设计为幂等动作或携带事务号，防止重传造成重复动作。

链路恢复后执行全量状态同步，重建 T113 状态镜像。

### 7.5 摄像机与交换机管理

- 摄像机按 ICMP、TCP、RTSP 三层检测，记录最接近业务的失败原因。
- ONVIF WS-Discovery 用于发现，Device/Media 接口用于补齐品牌、型号、序列号、MAC、Profile 和 OSD。
- SNMP OID 采用品牌/型号模板，将厂商数据映射为 CPU、内存、码率、温度、端口状态和流量等统一字段。
- 交换机优先使用标准 IF-MIB；厂商温度、PoE 等指标通过扩展模板配置。
- SNMP、RTSP、HTTP 和 ONVIF 优先使用项目镜像中可用的成熟库；若必须裁剪实现，应先确定协议子集并进行真实设备兼容测试。

一机一档的设备主键优先使用稳定设备标识或 MAC，IP 作为可变属性。摄像机口令只保存密钥引用，不存入普通档案导出内容。

### 7.6 自愈策略引擎

```text
ONLINE → SUSPECT → CONFIRMING → ACTION_PENDING
                                  │
                                  ▼
                         POWER_OFF → WAIT → POWER_ON → VERIFY
                              ▲                          │
                              └──── RETRY/BACKOFF ───────┘
                                           │ 超限
                                           ▼
                                      CIRCUIT_OPEN
```

每条规则包含：目标、触发条件、确认方式、动作、电源通道、断电时间、重试次数、冷却时间、时间窗口和熔断上限。

必须具备四道保护：

- 冷却：同一目标短时间不重复动作。
- 频率限制：限制每小时和每天次数。
- 上游关联：大量同类设备同时掉线时，优先判断交换机或主网故障。
- 熔断：超过上限只告警，等待人工恢复。

### 7.7 GPS 与时间

- GPS 可通过现有 `gpsd` 获取；镜像未提供时使用最小 NMEA 解析器。
- 首次有效定位立即上报，之后按位移阈值或最长周期上报。
- 无效定位保留最近有效坐标并附加无效标志。
- 时间源分为平台、GPS、NTP 和 RTC，优先级由产品要求配置。
- 小偏差渐进校正，大偏差在记录审计后设置，避免日志时间反复跳变。

### 7.8 门锁与门事件

- T113 发出带事务号的开锁请求，采集板负责脉冲和最终安全状态。
- 开锁需要来源鉴权、有效期、去重和审计。
- 门开事件立即上报，不等待抓图结果。
- 抓图和录像作为门事件的异步联动任务，失败单独告警。
- 断电后的锁状态由硬件安全要求决定，软件不得假设一定为上锁或开锁。

## 8. 媒体系统

### 8.1 抓图

```text
门事件/平台/Web → ivsboxd校验和去重 → IPC抓图任务
                 → ivsbox-media获取关键帧 → 临时文件
                 → fsync + rename → media.db索引 → 完成事件
                 → ivsboxd上报平台
```

- 网络摄像机优先使用 HTTP 抓拍接口，无法使用时从 RTSP 获取关键帧。
- 多路抓图默认串行或低并发，避免门开瞬间占满网络和 CPU。
- 生成缩略图供 Web 浏览，原图按保留策略存储。

### 8.2 断网录像

- 主网和平台均确认不可用并达到保持时间后启动录像。
- 网络恢复并稳定后延迟停止，防止链路抖动频繁启停。
- 默认使用码流直封装，不在 T113 上转码。
- 录像按 1～5 分钟切片，临时文件关闭成功后原子改名。
- 每路 RTSP 会话有建连、无数据、读写和停止超时。
- 同时录像路数、总输入码率和写盘带宽均设置上限。

### 8.3 视频读取

支持两种方式：

1. 设备主动向平台文件服务分片上传，适合 4G 私网和断网恢复补传。
2. 经授权远程通道访问设备 HTTP Range 下载，适合平台具备隧道能力的场景。

控制指令不得与大文件共享无优先级的发送队列。下载支持 Range、ETag/校验值、限速、超时和取消。

### 8.4 音频广播

媒体层定义统一 `audio_sink` 接口，支持：

- T113 ALSA/I2S 输出到功放和喇叭。
- 网络音柱或厂商协议。
- 摄像机语音回传能力。
- 采集板播放预置资源。

音频任务包含编码格式、音量、优先级、有效期和播放目标。优先级为紧急广播、告警、普通通知；高优先级可以抢占低优先级。实时音频必须配置抖动缓冲和无数据超时。

最终方案需要先确认功放/喇叭归属、平台编码格式、是否需要实时喊话和双向对讲。

## 9. Web 与远程访问

### 9.1 Web 功能

| 页面 | 内容 |
|---|---|
| 总览 | 版本、CPU/内存、网络、平台、UART、GPS、SD 卡 |
| 网络 | 双 WAN 状态、RTT/丢包、切换原因和历史 |
| 摄像机 | 在线状态、探测结果、SNMP、OSD和人工恢复 |
| 设备档案 | 品牌、型号、IP、MAC、序列号和最后在线时间 |
| 交换机 | 设备信息、端口和流量 |
| 媒体 | 图片、录像、下载、任务和存储水位 |
| 配置 | 网络、探活、SNMP、自愈、平台和上报参数 |
| 运维 | 日志、审计、升级、时间和诊断信息 |

### 9.2 API 原则

- 查询使用 `GET`，创建任务使用 `POST`，配置变更带版本号或 ETag 防止覆盖更新。
- 所有写操作必须鉴权并写审计日志。
- 高风险操作包括开锁、电源重启、升级、身份写入和远程代理授权，需要二次确认。
- API 返回稳定错误码，不将内部文件路径、口令或第三方库错误直接暴露给浏览器。

### 9.3 摄像机 Web 代理

- 仅允许访问设备档案中的 IP 和允许端口，禁止客户端传入任意目标地址。
- 处理 Host、Location、Cookie、WebSocket、超时和响应大小限制。
- 摄像机凭据在服务端使用，禁止发送给浏览器或写入 URL。
- 部分厂商页面包含绝对地址、私有插件或多端口访问，HTTP 反代无法保证兼容；这类设备使用受控四层远程通道，不承诺通过 URL 重写解决全部兼容问题。

### 9.4 远程访问通道

4G 私网下，平台主动访问设备 Web 和 HTTP 下载需要设备主动建立的反向通道。该能力必须满足以下前置条件：

1. 平台服务端实现相同的通道协议。
2. 客户确认允许远程进入设备管理网段。
3. 安全评审覆盖身份认证、授权、审计、限速和目标白名单。

推荐顺序：

1. 首选设备主动上传文件，减少远程入站面。
2. 已有成熟平台通道时复用其 SDK 或协议。
3. 无现成能力时，使用独立 TLS 连接实现受控多路复用，不与控制长连接争抢拥塞窗口。
4. 只有平台协议已支持流优先级和流控时，才允许复用控制长连接。

通道只接受 `web`、`camera:<id>`、`media:<id>` 等服务名，由本地白名单映射到目标；禁止接受任意 `ip:port`。授权令牌必须绑定设备、用户、服务和有效期，并记录连接时长及传输量。

新增通道命令属于平台协议升级，必须进行能力协商，不能同时宣称“平台无感知”。

## 10. 数据、配置和存储所有权

### 10.1 数据所有权

| 数据 | 唯一写入者 | 存储位置 |
|---|---|---|
| 运行状态镜像 | `ivsboxd` Reactor | 内存，必要字段定期快照 |
| 设备档案、自愈审计、网络历史 | `ivsboxd` | `/data/ivsbox/db/control.db` |
| 录像与抓图索引 | `ivsbox-media` | `/data/ivsbox/db/media.db` |
| 运行配置 | `ivsboxd` | `/data/ivsbox/config/` |
| 工厂身份 | `ivsbox-provision` | `/data/ivsbox/factory/` 或安全存储 |
| 媒体文件 | `ivsbox-media` | `/mnt/sd/ivsbox/media/` |
| 持久上报队列 | `ivsboxd` | `/data/ivsbox/queue/` |

Web 通过 IPC 查询数据，不直接读取 SQLite。每个数据库只有一个业务写入进程，避免跨进程写锁争用和所有权不清。

### 10.2 配置模型

配置按职责拆分：

```text
/data/ivsbox/config/
├── platform.json
├── network.json
├── cameras.json
├── snmp.json
├── recovery.json
├── media.json
└── web.json
```

- 每份配置包含 schema 版本、内容版本和校验值。
- 写入使用“临时文件 → `fsync` 文件 → `rename` → `fsync` 目录”。
- 更新前完成类型、范围、引用和跨字段校验。
- 应用失败时保留旧配置继续运行，并返回明确错误。
- 配置变更以事务方式通知相关模块；不能应用的字段不得部分生效。
- 密钥和口令保存到独立 `secure/` 目录，权限为 `0600`，普通配置只保存引用。

### 10.3 SQLite 策略

- 数据库位于内部可靠数据分区，不直接放在可拔出的 SD 卡。
- 使用 WAL、`busy_timeout`、短事务和定期 checkpoint。
- 网络时序数据按保留周期裁剪，避免无限增长。
- 媒体文件删除和索引删除使用可恢复事务：先标记、再删除文件、最后提交状态。
- 数据库损坏时保留原文件并重建索引，不覆盖现场证据。

### 10.4 SD 卡策略

- 检测未挂载、只读、空间不足、IO 错误和异常拔卡。
- 默认 85% 开始清理最旧且允许删除的录像，90% 限制新任务，95% 停止录像并告警，低于 70% 恢复。
- 抓图、告警录像、普通录像设置不同保留优先级。
- 不对每个媒体包调用 `fsync`；按分段关闭或可接受的时间窗口同步，平衡寿命和掉电损失。

## 11. OTA 架构

### 11.1 通用安全要求

升级包必须包含：产品型号、硬件版本范围、软件版本、最低可回滚版本、文件清单、SHA-256 和数字签名。设备内置只读公钥，签名校验为强制步骤。

下载、验签、空间检查、安装和切换由 `ivsbox-updater` 执行；`ivsboxd` 只接收命令、下载协调和上报进度。

### 11.2 应用级双版本

```text
/opt/ivsbox/releases/<version>/
/opt/ivsbox/current  -> releases/<current>
/opt/ivsbox/previous -> releases/<previous>
/data/ivsbox/ota/pending.json
```

流程：

1. 下载到临时目录并验签。
2. 解压到新版本目录，检查文件权限、架构和依赖。
3. 创建临时符号链接，通过 `rename` 原子替换 `current`。
4. 写入 pending 状态并重启业务服务。
5. 新版本通过平台、UART、配置、数据库和媒体 IPC 健康检查后确认。
6. 超时、连续启动失败或主动报告失败时，启动包装程序恢复 `previous`。

应用级回滚由启动包装程序/升级代理负责，不使用 U-Boot rootfs 槽位冒充应用回滚。

### 11.3 系统级 A/B

只有需要升级内核、DTB、驱动或 rootfs 时才启用系统级 A/B：

- 独立 boot/rootfs A、B 槽位。
- U-Boot 冗余环境、`bootcount`、`bootlimit` 和最后已知良好槽位。
- 可选采用 SWUpdate 或经过验证的 Tina/OpenWrt sysupgrade 方案。
- 系统槽位切换和应用版本组合必须在升级清单中绑定。

应用级双版本和系统级 A/B 是两条独立路径，不能在同一流程中混用未定义的符号链接和 U-Boot 槽位。

## 12. 日志、指标与健康

### 12.1 日志

- 所有进程写 syslog，由 logd 汇聚；持久日志由统一落盘策略管理。
- 日志字段至少包含时间、级别、进程、模块、事件码、请求号和设备目标。
- 运行日志按大小和数量滚动，故障及审计日志保留时间更长。
- 重复故障采用“首次、计数、恢复”方式抑制日志风暴。
- 日志禁止包含平台密钥、摄像机口令、SNMP community 和完整授权令牌。

### 12.2 指标

至少记录：

- UART 收发、校验错误、超时、重试和掉线次数。
- 平台连接、队列长度、未确认数量和补传结果。
- 每条 WAN 的 RTT、丢包、切换次数和持续时间。
- 摄像机探测、SNMP、RTSP 错误原因和耗时。
- 自愈触发、成功、失败、熔断和人工解除次数。
- 媒体会话、码率、写盘速度、失败和存储水位。
- IPC 队列积压、超时和拒绝次数。

### 12.3 看门狗

| 层级 | 机制 | 作用 |
|---|---|---|
| 服务级 | procd `respawn` | 进程崩溃后快速拉起 |
| 主控线程级 | 原子进度号 + 健康线程 | 主循环卡死时停止喂狗 |
| 系统级 | `/dev/watchdog` | 无法软件恢复时复位整机 |
| 媒体/Web | IPC 心跳 + 主动终止 + procd | 检测“进程存在但不响应” |

媒体或 Web 单独卡死时先重启对应进程，不直接复位整机。只有主控核心能力失效或重复恢复失败时才触发硬件复位。

## 13. 安全架构

### 13.1 权限划分

| 组件 | 权限 |
|---|---|
| `ivsboxd` | 串口、watchdog；按需授予 `CAP_NET_RAW`、`CAP_NET_ADMIN` |
| `ivsbox-media` | 媒体目录、音频设备，不授予网络管理权限 |
| `ivsbox-web` | 只访问自身资源和 IPC Socket，不访问串口、watchdog、配置密钥 |
| `ivsbox-updater` | root，只有升级时启动，输入必须先验签 |
| `ivsbox-provision` | 仅工厂模式可运行，量产后关闭入口 |

### 13.2 安全要求

- 平台连接使用 TLS 并校验证书；若现网暂不支持，形成明确整改项。
- Web 使用独立账号、会话超时、登录限速和角色权限。
- 开锁、重启、升级和远程通道采用短期授权并防重放。
- 摄像机代理和隧道只接受本地服务白名单，禁止开放通用代理。
- OTA 强制数字签名；CRC 和哈希不能替代发布者认证。
- 工厂身份写入后使用锁定位或受控维护流程，不以普通 Web 配置方式修改。
- 禁止通过 Web API 拼接执行任意 Shell 命令。

## 14. 技术选型原则

| 能力 | 首选 | 备注 |
|---|---|---|
| 构建系统 | GNU Make + 手写 Makefile | 不使用 CMake/Meson/Autotools/Ninja；编译机自带 make 4.3，无需额外安装 |
| 事件循环 | epoll/timerfd/signalfd/eventfd | Linux 原生 |
| 网络状态 | rtnetlink | 禁止轮询解析 `ifconfig` 文本 |
| 路由 | Netlink/策略路由 | 可用系统工具仅限安装和诊断，不作为主控制接口 |
| 串口 | termios | RS485 场景才使用 `TIOCSRS485` |
| DHCP/拨号 | BSP 已验证的 netifd、udhcpc、QMI/PPP 方案 | 不假定 ModemManager 一定存在 |
| GPS | BSP 已有 gpsd，否则最小 NMEA | 启动前做镜像能力盘点 |
| SNMP | 成熟轻量库或 net-snmp 裁剪 | 不把自研 BER 作为默认选择 |
| ONVIF | 成熟 SOAP/HTTP 组件 + 业务适配 | 用真实品牌样本做兼容测试 |
| RTSP/媒体 | FFmpeg libavformat 或 BSP 已有媒体栈 | 不通过 Shell 拼接长期运行命令 |
| HTTP | BSP 的 uhttpd 或成熟嵌入式 HTTP 库 | 选定一种并固化版本 |
| 数据库 | SQLite | 每个库单写入者 |
| 加密与签名 | mbedTLS/OpenSSL + Ed25519/ECDSA | 复用项目现有可信密码库 |
| 守护 | procd | Tina/OpenWrt 原生 |

立项初期必须生成 BSP 能力清单，确认 FFmpeg、ALSA、SQLite、TLS、cgroup、nftables、gpsd 和 4G 拨号栈是否真实存在。架构不得把“Linux 常见组件”直接等同于“当前固件已具备”。

## 15. 工程结构与构建体系

### 15.1 目录结构

```text
ivsbox/
├── Makefile                    顶层唯一构建入口
├── mk/
│   ├── toolchain-host.mk       宿主机工具链（gcc / ASan）
│   ├── toolchain-tina-arm.mk   板端交叉工具链（arm-linux-gnueabi-*）
│   └── rules.mk                通用编译、归档、链接、依赖规则
├── include/ivsbox/
├── src/
│   ├── app/
│   ├── core/
│   ├── hal/
│   └── modules/
├── media/
├── web/
├── updater/
├── tools/
│   ├── provision/
│   ├── uart_simulator/
│   └── platform_simulator/
├── tests/
│   ├── unit/
│   ├── fuzz/
│   └── integration/
├── packaging/
├── scripts/
│   ├── init.d/
│   └── ci/
└── docs/
```

### 15.2 构建系统：只用 GNU Make

构建系统固定为 **GNU Make + 手写 Makefile**，不引入 CMake、Meson、Autotools、Ninja 或任何构建生成器。

选择理由：

1. 编译机（Tina 配套 Ubuntu 22.04）**自带 make 4.3**，`arm-linux-gnueabi-gcc` 已在 `PATH`；而 cmake 需额外 `apt-get install`。少一层外部依赖就少一类环境差异。
2. 本工程是单一语言（C11）、单一日标形态（可执行文件 + 静态库），没有需要构建生成器提供的能力（编译器特性探测、包查找、多配置矩阵）。
3. Makefile 把编译与链接过程完全暴露，嵌入式调试时不需要穿过生成层排查。
4. 板端不做增量构建，产物由 `packaging/` 统一归集。

约束：

- 顶层 `Makefile` 是唯一入口，`mk/` 只放被 `include` 的片段。**不使用递归 make**（各子目录不放 Makefile），源文件按目录集中声明，避免变量作用域和并行构建的隐患。
- 需要派生的内容（版本头、依赖关系）用 Make 变量与规则表达，禁止在构建过程中调用脚本再生成一层构建脚本。
- `make` 不带参数即宿主机 Debug 构建，这是开发时最常用的路径。

### 15.3 两类目标与命令

| 目标 | 命令 | 工具链 | 产物目录 | 用途 |
|---|---|---|---|---|
| Host | `make` | 宿主机 `gcc` | `build/host/` | 单元测试、模糊测试、ASan/UBSan、静态分析 |
| ARM | `make arm` | `arm-linux-gnueabi-*` | `build/arm/` | 上板运行的板端产物 |

交叉编译只需选择目标，不再需要工具链文件：

```sh
make arm                                          # 用 PATH 中的 arm-linux-gnueabi-
make arm CROSS=arm-linux-gnueabi-                 # 显式指定工具链前缀
make arm SYSROOT=/opt/EmbedSky/Tina/.../sysroot   # 需要时指定 sysroot
```

| 变量 | 默认值 | 说明 |
|---|---|---|
| `TARGET` | `host` | `host` / `arm` |
| `CROSS` | `arm-linux-gnueabi-` | 交叉工具链前缀，`TARGET=arm` 时生效 |
| `SYSROOT` | 空 | 交叉编译 sysroot，留空用工具链默认 |
| `BUILD_DIR` | `build/$(TARGET)` | 产物目录 |
| `DEBUG` | host 为 `1`，arm 为 `0` | 是否带 `-g` 并保留断言 |
| `SANITIZE` | 空 | 非空即追加 ASan/UBSan |

其它目标：

| 命令 | 作用 |
|---|---|
| `make test` | 宿主机构建并运行全部单元测试 |
| `make asan` | 宿主机 ASan/UBSan 构建并运行单测 |
| `make fuzz` | 构建模糊测试目标 |
| `make analyze` | `-fanalyzer` 静态分析；外部工具缺失时提示并跳过 |
| `make package` | 把板端产物归集到 `packaging/` |
| `make clean` | 删除 `build/` |
| `make help` | 列出全部目标与可覆盖变量 |

### 15.4 编译与链接配置

统一选项写在 `mk/rules.mk`：

```make
CFLAGS_COMMON  := -std=c11 -Wall -Wextra -Werror -ffunction-sections -fdata-sections
LDFLAGS_COMMON := -Wl,--gc-sections -Wl,--no-undefined
```

- `-MMD -MP` 生成 `.d` 依赖文件，`-include $(DEPS)` 引入，改头文件自动重编；不引入 `makedepend` 等额外工具。
- 中间产物与最终产物全部落在 `$(BUILD_DIR)`，源码目录不留 `.o`。
- `TARGET=arm` 时工具链前缀、`--sysroot`、库搜索路径由 `CROSS`/`SYSROOT` 推导；`TARGET=host` 时叠加 `-D_GNU_SOURCE` 便于本机跑单测。
- 头文件搜索路径只有工程 `include/` 和 `mk/` 声明的第三方路径，不隐式继承系统路径差异。

### 15.5 库分层与依赖方向

依赖方向用链接顺序显式写死，不允许“能编过就算”：

```make
LDLIBS_ivcore    :=
LDLIBS_ivhal     := -livcore
LDLIBS_ivmodules := -livcore -livhal
LDLIBS_ivsboxd   := -livmodules -livhal -livcore
```

| 库 | 内容 | 允许依赖 |
|---|---|---|
| `libivcore.a` | 事件、队列、定时器、日志、配置、状态机、CRC | 仅 libc |
| `libivhal.a` | serial、netlink、watchdog、fs、clock、capability | `libivcore` |
| `libivmodules.a` | 12 个业务模块 | `libivcore`、`libivhal` |
| `ivsboxd` | 链接以上三库，装配在 `src/app/` | — |

- 链接顺序即依赖方向，配合 `-Wl,--no-undefined`，反向依赖在链接期即暴露。
- 新增模块只需在顶层 `Makefile` 的源文件列表里登记，不新建子 Makefile。
- `media/`、`web/`、`updater/`、`tools/` 各自是独立可执行目标，复用同一套 `mk/`。

## 16. 测试与验收体系

### 16.1 自动化测试

全部测试通过 `make test` / `make asan` / `make fuzz` 驱动，不依赖任何 IDE 或构建生成器。

| 类型 | 内容 |
|---|---|
| 单元测试 | 帧解析、配置事务、探活统计、网络切换、自愈规则、IPC 编解码 |
| 模糊测试 | UART/平台/IPC/ONVIF/SNMP 输入边界、半包、粘包和超长数据 |
| 集成测试 | UART 模拟器、平台模拟器、SQLite、主控与媒体 IPC |
| 设备兼容 | 各品牌摄像机 ONVIF、RTSP、SNMP、Web 代理 |
| 故障注入 | 弱网、双网断开、串口拔出、SD 卡满/拔卡、进程卡死、掉电 |
| 安全测试 | 鉴权绕过、重放、SSRF、任意目标代理、恶意升级包 |
| 长稳测试 | 72 小时以上，多轮网络切换和媒体启停，监测内存及句柄增长 |

### 16.2 核心验收场景

1. 无线失败后切换有线，平台连接从新接口重建，未确认数据不重复且不丢失。
2. 无线恢复达到稳定期后回切，媒体大流量在稳定前不抢占控制链路。
3. 摄像机故障经过多层确认后才重启，达到上限后熔断。
4. 门开立即上报，抓图异步完成，抓图失败不影响门事件。
5. 主网中断触发录像，网络恢复后正确封存片段。
6. SD 卡拔出或写满时，控制和平台业务继续运行并产生明确告警。
7. Web、媒体单独卡死时只重启对应服务；主 Reactor 卡死时硬件狗复位。
8. 应用升级失败恢复上一版本；系统 A/B 断电测试能够回滚正确槽位。
9. 未签名或型号不匹配的升级包必须被拒绝。
10. 远程代理无法访问白名单之外的 IP 和端口。

## 17. 实施路线图

| 阶段 | 交付内容 | 退出条件 |
|---|---|---|
| M1 基础骨架 | Makefile 构建体系、Reactor、IPC、日志、配置、数据库、procd、测试框架 | `make` 与 `make arm` 两类构建通过，IPC 和看门狗可验证 |
| M2 核心链路 | UART、平台协议、GPS、状态镜像、模拟器 | 丢包、半包、重连、重同步测试通过 |
| M3 网络管理 | 统一探活、多 WAN、持久队列 | 拔线、弱网、回切和补传验收通过 |
| M4 设备管理 | ONVIF、RTSP探测、SNMP、一机一档、自愈、门锁 | 目标品牌兼容矩阵通过 |
| M5 Web与工装 | 页面、API、配置、日志、出厂写入 | 权限和审计测试通过 |
| M6 媒体与音频 | 抓图、录像、索引、下载、存储水位、广播 | 资源上限和异常恢复通过 |
| M7 OTA与加固 | 应用双版本、可选系统A/B、签名、故障注入 | 断电回滚和72小时长稳通过 |
| M8 远程通道 | 平台配套、隧道、代理兼容和安全测试 | 仅在平台立项后执行 |

## 18. 编码前必须冻结的接口

1. 平台帧格式、命令字、应答、幂等、重传和离线补传规则。
2. 采集板 UART 帧、校验、命令应答、门锁、电源和门事件语义。
3. 无线接口类型、4G 模组型号、拨号栈和双 WAN 的真实网卡名称。
4. 摄像机和交换机品牌、数量、RTSP 地址、ONVIF 能力和 SNMP OID。
5. 同时录像数量、码率、SD 卡容量、文件系统及保留周期。
6. 音频输出硬件、编码、实时性、广播目标和双向对讲要求。
7. OTA 范围是应用、采集板固件还是完整系统镜像。
8. 远程访问是否需要平台改造，以及客户网络安全边界。
9. Tina 镜像现有组件、内核配置、Flash/内存/CPU预算和许可证限制。

## 19. 最终架构结论

本系统采用三个常驻业务进程和两个按需工具：

- `ivsboxd` 保持小而稳定，集中处理控制、通信和权威状态。
- `ivsbox-media` 隔离 FFmpeg、RTSP、存储和音频带来的资源及崩溃风险。
- `ivsbox-web` 隔离用户交互、文件下载和摄像机代理风险。
- `ivsbox-updater` 以最小特权窗口处理可信升级。
- `ivsbox-provision` 将量产身份写入与运行期配置彻底分离。

主控以单 Reactor 保证状态一致性，以有界 worker 承担不可避免的慢操作；进程间通过有消息边界、可版本化和可鉴权的本地 IPC 协作。网络切换、UART兼容、OTA回滚、数据所有权和远程访问均具有明确边界，不依赖隐含行为。构建体系只依赖 GNU Make，工程结构与依赖方向对使用者完全可见。

该架构既保留 Linux 原生能力和完整功能覆盖，也将一期实现复杂度、安全风险与后续维护成本控制在可验证范围内。
