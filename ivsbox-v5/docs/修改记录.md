# IVSBox 修改记录

> 本文件是项目**唯一的修改历史权威记录**（即 `xiugaijilu.md`，不另建同名文件），规则见项目根目录 `AGENTS.md` 规则 2。
> 记录顺序：**倒序**（最新记录在最上方），只追加，不修改、不删除历史条目。
> 时间格式：`YYYY-MM-DD HH:mm`

---

## 记录格式模板

```markdown
## YYYY-MM-DD HH:mm

- **修改文件**
  - `相对路径/文件1`
  - `相对路径/文件2`
- **问题 / 目的**：一句话说明这次修改要解决什么问题或达成什么目的。
- **处理方式**：简述做法。
- **验证结果**：如何验证、结果如何。
```

---

## 2026-09-24 11:56

- **修改文件**
  - `ivsbox-v5/docs/开发流程.md`（新增）
  - `AGENTS.md`（规则 7 文档清单与关联文档表中"功能开发列表"行替换为"开发流程"）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户删除全部历史开发文档后，要求基于《系统架构设计 v5 整合版》重新梳理一份简洁的开发流程文档，只写每步该做什么。
- **处理方式**：按架构文档 §17 路线图 M0~M8 编写：每阶段一张表（做什么/要点）+ 退出条件，另立通用规则（开发闭环、代码约束、选型纪律、收尾四件事）；M8 标注暂缓。经用户本次消息主动要求新建（规则 7 第 2 条），并在 AGENTS.md 登记。
- **验证结果**：纯文档修改，无代码产出（规则 4 不适用）。内容与架构文档 §17/§18/§16.2 逐条对应。

## 2026-09-24 11:53

- **修改文件**
  - `ivsbox-v5/docs/IVSBox-功能开发列表.md`（删除）
  - `ivsbox-v5/docs/README.md`（删除）
  - `ivsbox-v5/docs/bsp-capability.md`（删除）
  - `ivsbox-v5/docs/代码说明.md`（删除）
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（删除）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户判定上述 5 个文档为无用产物并已手动删除，要求不参考历史文档，基于架构文档重新编写开发流程。
- **处理方式**：登记用户本人的删除动作并提交入库。在 main 直接提交的原因：删除是用户在主工作副本上的手动操作，非 AI 会话内改动，无法纳入 stint 会话。
- **验证结果**：`git status` 删除项与登记一致。

## 2026-09-24 11:55

- **修改文件**
  - `AGENTS.md`（**规则文件自身修订**，属规则 8 例外第 1 类，直接在 main 工作副本修改；本次只改与文档体系无关的表述与事实）
    - 规则 2：删除"在该文件**末尾**追加"的表述（与 `修改记录.md` 自身规定的"倒序"直接矛盾），改为"按该文件自身规定的顺序新增（当前为倒序：插入最上方、紧接格式模板）"；时间要素明确**必须是本机当前时间**，禁止预计时间、占位时间、晚于当前时刻
    - 规则 3：新增"**本机推送环境**"四条实测事实——必须走本地代理 `127.0.0.1:7897`（`52667` 实测不通）、单次 push 约 50 秒不得用短超时判失败、凭据非交互下静默失败需 `GIT_TERMINAL_PROMPT=0`、推送后用 `git ls-remote origin main` 与本地 `HEAD` 比对
    - 规则 8：新增"**降级路径**"小节——`git-stint` 0.6.1 本机高频失败（`spawnSync git EBUSY`、`refs/heads/stint/*` 声称成功却不落盘），改用 `git worktree add .stint/<会话名> -b stint/<会话名>` 等价隔离，含清理步骤与人工防重叠自查；收尾判据拆为 git-stint / 降级两路；"已知限制"补一条兜底判据（不单靠 `git stint list`）
    - 关联文档表：补登记 `.gitattributes`
    - 文件头：追加 2026-09-24 修订说明行
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：`AGENTS.md` 是全部 AI 共用的规则源，却存在事实性错误与失效指引——① 规则 2 要求"末尾追加"，而唯一记录文件规定倒序，实际执行中已出现排序混乱（本文件第 24/33/42/53 行为 11:45→11:50→11:40→12:20，第 336/348 行为 16:57→16:15）；② 规则 8 只给本机高频失败的 `git-stint` 路径，导致每次开会话都要白试一遍；③ 规则 3 缺本机推送环境事实，曾因短超时把成功的 push 误判为失败。
- **处理方式**：按"只改与文档体系无关的表述与事实"原则修订 6 处。**涉及已删文档的引用（`代码说明.md`、`IVSBox-功能开发列表.md`、`S02-core基础设施操作手册.md`，共 7 处）与规则 1"实现记录"的落点本次一律不动**，等新文档定稿后统一处理。
- **验证结果**：纯文档修改，无代码产出（规则 4 不适用）。改后已通读全文，除已知的已删文档引用外无其他相互矛盾。**配套改动暂不提交**（用户指示：等新文档定稿后一并提交），故本记录此刻处于未提交状态。
- **并发情况**：本次改动期间（11:55:17）并发会话提交了 `a096bb1`（`docs: 清理无用文档并登记删除动作`），5 个无用文档的删除已随之入库，本文件顶部另有其 11:53 条目。本记录按倒序插入该条目之上，**未改动其任何内容**。注意 `a096bb1` 目前**尚未推送**（远端 `main` 仍为 `8784ecc`）。

## 2026-09-24 11:45

- **修改文件**
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（**推倒重写**：删除已完成事项（CRC、环形缓冲条目）、决策引用块、头部说明全部；任务重新从 1 连续编号——1 配置读写 / 2 日志接 syslogd / 3 单实例锁 / 4 main 接线；只保留待办步骤的"做/测/上板"与收尾动作）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户指示"重头开始，把已经完成的全部删掉，重新写"——操作手册只描述**待执行的工作**，已完成/已删除的任务不应出现在手册里。
- **处理方式**：全文重写，已完成内容（CRC 步骤、环形缓冲条目与附录）彻底移出；标题从"操作卡片"改回"操作手册"；历史决策仍可查（`bsp-capability.md`、`修改记录.md` 此前条目）。
- **验证结果**：纯文档修改。提交范围 2 个文档。

## 2026-09-24 11:50

- **修改文件**
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（第二次精简：240 → 105 行。改为**操作卡片**风格——每步只留"做/测/上板"三段；删除 §2"平台事实与需求修正"全部内容与决策表格，改为文件头两行指向 `bsp-capability.md` 与 `修改记录.md`）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户再次反馈"还是太啰嗦，（§2 平台事实）这些都他妈的有什么用，谁有时间去看这玩意，有没有简洁版的"——手册读者是执行者，不是评审者；查证材料应放归档文件，不放操作现场。
- **处理方式**：重写为卡片式；被删内容的知识去向后——syslogd 实测事实：`bsp-capability.md` §2.8；环形缓冲决策 D3/D4 论证：`bsp-capability.md` §7 与本文件 2026-09-24 11:06/12:20 两条；选型规则：开发列表 §6.11 与 `bsp-capability.md` §7.6。所有知识仍可查，只是不在干活时挡路。
- **验证结果**：纯文档修改。提交前 `git status` 仅 2 个文档改动。

## 2026-09-24 11:40

- **修改文件**
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（**精简重写**：430+ 行 → 约 230 行。只保留"要实现哪几步、每步怎么做、怎么验证"；决策论证与查证过程压缩为指向 `bsp-capability.md` §7 / 决策 D1~D4 的引用；附录 A 压缩至 20 行）
  - `ivsbox-v5/docs/IVSBox-功能开发列表.md`（§6 纪律**新增第 11 条**：依赖选型规则——优先现成 Linux 库 → 没有 → 先自研保进度 → 登记待安装清单 → 做镜像时加入并替换）
  - `ivsbox-v5/docs/bsp-capability.md`（**新增 §7.6 待安装库清单**：依赖选型纪律的登记处，含登记模板，暂无条目）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户反馈 S02 手册"写的太啰嗦了，只关心需要实现那几步，每一步该怎么做，该如何验证"；并确立新的选型总规则："处理代码优先使用现成的 Linux 开发库，如果没有，先用代码自己实现，然后记录需要安装的库，等后面生成镜像时添加进去"。
- **处理方式**：手册按"步骤 → 文件 → 怎么做（含关键代码） → 验证（主机/板端判读标准）"骨架重写，删除背景论述与重复论证；新选型规则落两处——开发列表 §6 纪律第 11 条（对所有后续任务生效）、`bsp-capability.md` §7.6（登记处）。手册头部注明选型总规则与登记处指向。
- **验证结果**：纯文档修改，无代码产出（规则 4 不适用）。提交范围仅上述 4 个文档；提交前 `git status` 自检无任务外文件。

## 2026-09-24 12:20

- **修改文件**
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（整体修订：§1.1/§1.2 现状表更新；§2.1 日志口径按板端实测校正；§2.6 决策 D3 由"暂缓"改为"**删除**"；**新增 §2.7 决策 D4**；§3.1 明端口令改占位符；§3.2/§8 会话隔离改用 worktree 降级方案；§4 步骤 2 改为删除说明 + 替代接收代码、步骤 3 改用 `getline`/`open_memstream`、步骤 4 补 syslogd 轮转口径、步骤 6 新增单实例锁；§5 测试项 5→4；§7/§9/§10/§11 同步；**新增附录 A** 存档原环形缓冲设计）
  - `ivsbox-v5/docs/bsp-capability.md`（§2.6 JSON 库行按决策 D1 更正；§2.8 新增 busybox syslogd 三条实测事实）
  - `ivsbox-v5/docs/IVSBox-功能开发列表.md`（§3-S02 原文按决策 D1/D2/D3 更正；§10 实现记录追加 S02.2 / S02.3 两行并更新 S02 行）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户提出"S02 不是写死的，如果你有更好的 Linux 方案，可以告诉我，然后再确定更改"。本轮把 S02 七项按"Linux 原生做法"重新体检，逐项给出**建议删除 / 建议改写 / 保持不动**三级结论，并在用户确认后落盘。核心判断：七项里**只有"环形缓冲"这一项是错的**（用户态重造内核已有的字节缓冲），"日志"与"配置读写"属做多了，其余四项本就正确。
- **处理方式**：用户经选项确认三项决策 —— ① 步骤 2 从 S02 **删除**（原设计降为附录 A 存档，仅在 UART 读跨线程时才启用，且届时必须把 `volatile` 换成 C11 `stdatomic` 的 acquire/release）；② 四条 Linux 原生小件（单实例 `flock`、`tcflush`、`clock_nanosleep`、收包对账统计）**全部纳入本轮 S02**；③ 线程模型**定为**"串口 `read()` 在主 Reactor 线程"，作为后续硬约束写入 §2.7。另按板端实测校正日志口径，并顺手消除"bsp-capability 说自带 cJSON、S02 手册说不引入 cJSON"这处跨文档矛盾（规则 7 单一事实源）。
- **验证结果**：本次为纯文档修改，无代码产出（规则 4 不适用）。板端事实经实测取得：`/etc/init.d/S01syslogd` 的 `SYSLOGD_ARGS=""`、`syslogd --help` 自述 `ignores /etc/syslog.conf`（该文件实测不存在）、`-s` 默认 200KB、`-b` 默认 1、BusyBox v1.33.2 —— 均通过 `ssh tqt113` 中转 `root@192.168.2.105` 读取，全程免密。提交范围仅上述 4 个文档。

## 2026-09-24 11:06

- **修改文件**
  - `ivsbox-v5/docs/bsp-capability.md`（新增 §7「第三方库可用性清单（交叉 sysroot 与板端 rootfs 对照）」；原 §7 实现记录顺延为 §8 并追加一行）
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（新增 §2.6 决策 D3；§4 步骤 2 标注"暂缓执行"并加论证引言；§11 实现记录追加一行）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户指示"继续处理上一个问题，使用现有的库处理环形缓冲区问题（步骤 2：SPSC 环形缓冲）"。上一轮已确认决策 D2 的口径是"优先使用现有 Linux 开发库"（CRC 走 zlib），本轮需按同一口径回答步骤 2：**平台上到底有没有可用的环形缓冲库**；并回答用户反问的"**接收数据有没有必要做环形缓冲**"。目的是把选型体检结论与决策归档，避免后续 S04/S06 重复调研或误选。
- **处理方式**：
  1. **平台两侧逐库实测**（不猜）：VM 内扫交叉 sysroot（`find` + `pkg-config` 清单）、板端扫 `/lib` + `/usr/lib`（经 VM 中转，免密）。结论：**两侧都没有任何无锁/环形缓冲库**（liburcu、concurrencykit `ck_ring`、liblfds、DPDK `rte_ring`、libkfifo 全无）；libubox 只有 `avl/blob/blobmsg/kvlist/list/runqueue/safe_list/uloop/usock/ustream/utils/vlist`；板端"已有"的 glib `GQueue`/`GAsyncQueue` 与 GStreamer `GstAdapter` 均为**有锁 + 堆分配**，不满足步骤 2 的"静态存储、不动态 malloc"纪律。
  2. **公开候选逐个体检**：`szanni/ringbuf`（单头文件、C11 原子、ISC 许可）形状最近但 `ringbuf_new()` **内部堆分配且无 caller 传缓冲 API**（与本手册纪律正面冲突），且 2020 年后停更、其并发测试提交信息自述"still seem to fail randomly"；Zephyr `spsc_lockfree.h` **编译期静态数组**、形状最对，但绑 Zephyr 原子原语（`z_spsc_in/out`、`BUILD_ASSERT`），移植成本 ≥ 自研；`RomanHorshkov/SPSCring` 只存 `int` 且动态分配。**三者均不采用**（公开来源已在文档 §7.5 标注，天嵌官方资料不涉及通用 C 库选型）。
  3. **回答"要不要做"**：串口按架构 §7.4 纳入主 Reactor，`O_NONBLOCK` + epoll 下每轮 `read()` 到 `EAGAIN` 并直接喂流式帧解析器（解析器自带半包累积）即可，**内核 tty 翻转缓冲已是缓冲，多加一层环只是多一次拷贝** → 判定"**接收数据本身不需要环**"。仅三种情况才需要：① UART 读**跨线程**（独立线程 / 厂商 SDK 回调线程）——此时才需要跨线程通道与原子操作；② 主循环可能长时间阻塞、需"快吸慢解"（架构已有"有界慢任务池"防此，属保险）；③ 需保留最近 N KB 原始字节做现场留痕（与"接收"无关）。
  4. **连带护栏**：环**不能防丢、只把溢出点后移**；而板端 `/proc/tty/driver/uart`（sunxi 驱动）实测只报 `tx:`/`rx:` 累计、**无 overrun / frame-error 计数** → 丢字节无法从驱动层观测，故应先做应用层统计（`read()` 字节数、出帧数、半包数、CRC 错、重同步次数）并与 `rx:` 累计比对。
  5. **归档位置**：表格化平台清单写进 `bsp-capability.md` §7（该文档本就是 M0 BSP 能力清单，且缺口清单记载 v5 删除了 v4 的"附录 A 依赖清单（含体积/可否省略）"，本节即其实测替代）；决策写进 S02 手册 §2.6（沿用该手册 §2.3 决策 D1、§2.5 决策 D2 的既有位置与表格体例）。**未新建文档**（规则 7 第 2 条）。
- **验证结果**：纯文档改动（本轮不含代码产出，按规则 4 第 4 条不追加 `代码说明.md`）。所有平台结论来自实测命令输出，非推测；对无法确证的两处（`libsqlite3.so.0.8.6` 的精确版本、Paho/libmodbus 头文件补齐所需的具体版本号）已在文中明确标注"待查/待确认"，符合规则 5。本轮未改动任何代码文件，`ivsbox-v5/src`、`include`、`tests` 与 CMake 均未触碰。
- **规则 8 合规性**：全部改动在会话 `wb-libsel` 的 worktree（`.stint/wb-libsel`，分支 `stint/wb-libsel`）内完成，未在 main 工作副本直接编辑。git-stint 本机仍不可用（`spawnSync git EBUSY`，同本日 09:25 记录），沿用等价的 `git worktree` 方案。所改章节与另一 AI 会话 `trae-s02`（其 082ff27 改了 S02 手册 §1.1/§2.5/§4 步骤 1/§5/§9/§11）**存在同文件但不重叠**：本次改 §2.6（新增）、§4 步骤 2（其改的是步骤 1）、§11（追加行）；合并时按"双方记录都保留、按时间倒序"处理。
- **已知待办（本次未动，留待用户决定）**：S02 手册 §3.1 表格中仍写着板端口令明文"root / 123456"，与规则 3.6（口令禁止入库）不符，且该口令未经本次实测确认（本机 VM→板子走密钥免密，无需口令）；建议改为占位符，需用户确认后处理。

---

## 2026-09-24 10:07

- **修改文件**
  - `ivsbox-v5/include/ivsbox/iv_crc.h`（新增）
  - `ivsbox-v5/src/core/iv_crc.c`（新增）
  - `ivsbox-v5/tests/unit/test_crc.c`（新增）
  - `ivsbox-v5/CMakeLists.txt`（ivcore 挂 ZLIB 依赖）
  - `ivsbox-v5/tests/CMakeLists.txt`（新增 test_crc 目标）
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（§1.1 / §2.5 新增决策 D2 / §4 步骤 1 重写 / §5① / §9 / §11）
  - `ivsbox-v5/docs/IVSBox-功能开发列表.md`（§10 新增 S02.1 行、S02 行说明更新）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
  - `ivsbox-v5/docs/代码说明.md`（新增 2026-09-24 10:07 节）
- **问题 / 目的**：执行 S02 步骤 1（CRC）。用户决策 D2（2026-09-24）：CRC 不手写多项式位运算，改用现有 Linux 开发库。原开发列表"CRC8/CRC16 自己写 20 行"被 D2 覆盖；现网帧实际 CRC 类型待 S06 确认帧格式，确认前不预写变体。
- **处理方式**：新增 `ivs_crc32()` 薄包装 zlib `crc32()`（支持分段累计，对应 S06 半包场景）；根 `CMakeLists.txt` 仅 UNIX 下 `find_package(ZLIB REQUIRED)` 并给 `ivcore` PUBLIC 链接 `ZLIB::ZLIB`（Windows MinGW 主机无 zlib，不编 `iv_crc.c` 以免破坏主机验证）；`tests` 新增 `test_crc`（4 条 CRC-32/ISO-HDLC 公开向量 + 分段==一次性一致性）。动手前先实测平台事实：板端 `/usr/lib/libz.so.1.2.11` 在位、交叉 sysroot `usr/include/zlib.h` + `usr/lib/libz.so` 在位、VM `zlib1g-dev` 已安装。
- **验证结果**：VM 主机 `ctest` 2/2 全绿（ipc_hdr + crc，`100% tests passed`），`test_crc` 直跑 4 条向量全 PASS（`"123456789"`→`0xCBF43926` 等，`DIRECT_EXIT=0`）；ARM 交叉编译通过（CFG/BUILD 退出码均 0，产物 ARM EABI5 动态链接）；新 `ivsboxd`（20828B，较 S01 +88B）scp 上板经 COM4 运行：三行版本输出正常、`mono_clock_ready=yes`、`RUN_EXIT=0`，证明板端 zlib 运行时依赖成立。
- **规则 8 合规性**：全部改动在会话 `trae-s02` worktree 内完成；VM 侧验证以 scp 单文件同步（VM 仓库工作区状态未动 git）。手册 §7 由 wb-envcheck 会话更新（serial_run.py 引用）与本会话所改章节（§1.1/§2/§4 步骤 1/§5/§9/§11）不重叠。

---

## 2026-09-24 09:37

- **修改文件**
  - `ivsbox-v5/docs/操作手册/日常开发闭环流程.md`（新增 §8.1 环境自检实测；重写 §6.4 口令相关；新增 §6.5 console 现状；§13 实现记录追加一行）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户要求"编写 hello world 程序，测试开发环境"。目的是在进入 S02 之前，用最小程序把 §8 那条"改代码 → 编 → 推 → 跑"的链路端到端实测一遍，确认 VM 主机编译、ARM 交叉编译、SCP 上板、COM4 串口四个环节都是通的，并把结论沉淀下来供后续任务复用。
- **处理方式**：
  1. 写一次性自检程序 `hello.c`（`_GNU_SOURCE` + `printf` 架构/指针位宽/编译器版本/glibc 版本/`CLOCK_MONOTONIC`，输出全 ASCII 以适配板端 GBK 控制台），放在**本机 gitignore 覆盖的临时目录**（非仓库路径），未入库。
  2. 按新文档 §8 的链路实测：`scp` 到 VM → VM 内 `gcc` 主机编译并运行 → `arm-linux-gnueabi-gcc --sysroot=$(... -print-sysroot)` 交叉编译并 `file`/`readelf` 校验 → VM 内 `scp` 推板子 → Windows COM4 用 `serial_run.py` 下发执行并用 `sanitize.py` 洗控制字符后读回。
  3. 把实测结论回写文档：新增 §8.1（含自检源码、四段命令、实测输出表、五条结论、清理步骤），并按实测事实修正 §6.4（**VM→板子密钥免密已配好**、VM **无 `sshpass`**、免密失效时如何恢复）与新增 §6.5（板端 console 已处于 root 登录态 `root@TQ113:/tmp #`，COM4 下发命令无需口令）。
  4. 按规则 6 清理全部临时物：板端 `/tmp/hello_arm`、VM `/tmp/envcheck/`、本机临时工作目录（含 `hello.c`、`hello_host`、`serial_*.txt`、`_*.log`）。
- **验证结果**：**四段链路全部通过，且工具链与板端版本严格匹配**——
  - 主机侧：`ptr_bits=64` / `glibc=2.35` / `HOST_EXIT=0`（Ubuntu 22.04 自带 gcc 11.4.0）；
  - ARM 产物：`ELF 32-bit LSB executable, ARM, EABI5 version 1 (SYSV), dynamically linked, interpreter /lib/ld-linux.so.3`，`NEEDED: libc.so.6`；
  - 板端：`ptr_bits=32` / `target_arch=arm-eabi` / `compiler=7.3.1 20180425 [linaro-7.3-2018.05 ...]` / **`glibc=2.25`** / `mono_ms=1789774` / `hello_exit=0` / `RUN_EXIT=0` / `uname -m=armv7l`；
  - 关键吻合：板端 `/lib/ld-linux.so.3 -> ld-2.25.so`，与交叉 sysroot 的 glibc 2.25 完全一致 → 交叉产物可直接上板运行；
  - 传输校验：`hello.c` 两侧 md5 一致（`b5079d27…`）、`hello_arm` 两侧 md5 一致（`2266f6f1…`）；
  - 另查明：板端 busybox 的 `ldd --version` 无输出，**判断 glibc 版本要看 `/lib/ld-*.so` 指向**，已写进 §8.1。
  - 本次为纯文档改动（自检程序未入库，故无项目代码改动），按规则 4 第 4 条不追加 `代码说明.md`。
- **规则 8 合规性**：本次改动在隔离 worktree `.stint/wb-envcheck`（分支 `stint/wb-envcheck`）内完成，未在 main 工作副本直接编辑；git-stint 本机仍不可用（同 09:25 记录，`spawnSync git EBUSY`），沿用等价的手工 `git worktree` 方案。

---

## 2026-09-24 09:25

- **修改文件**
  - `ivsbox-v5/docs/操作手册/日常开发闭环流程.md`（新建）
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（§7 串口脚本名与调用方式更正）
  - `AGENTS.md`（关联文档表新增"日常开发闭环流程"一行）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户要求把日常操作流程沉淀成文档：SSH 连虚拟机 → 在虚拟机内编译 → SCP 推文件到开发板 → COM4 串口连接开发板通信。此前仓库只有"任务级"手册（`S02-core基础设施操作手册.md`）与"环境搭建"文档，**没有一条任务都复用的通用链路说明**，每换一个任务都要重新拼命令；同时 `S02-core基础设施操作手册.md` §7 引用的串口脚本名 `serial_send.py` 与实际脚本 `serial_run.py` 不符、参数形式也写错（实际为位置参数），照抄会直接失败。
- **处理方式**：
  1. 经用户确认后新建 `操作手册/日常开发闭环流程.md`（规则 7 第 2 条要求新建文档先征得同意，本次已确认；用户同时选定"SCP 经编译 VM 中转"与"口令一律占位符"两项口径）。文档共 13 节：三方角色与链路图（Windows 只做编辑/git/COM4，VM 做单测与交叉编译，板子只运行）、分工依据（本机无 cmake/make/clang、工具链在 VM `/opt/EmbedSky/Tina` 且已在 PATH、VM 与板子同网段、板子仅 220MB 内存且无 gcc）、实测事实速查表（tqt113=192.168.2.115、板子 192.168.2.105、COM4/CH340/115200、`/tmp` 与 `/opt/ivsbox/config/`、busybox syslogd）、四个步骤详解（SSH 登录与代码对齐；`build/host` ctest → `build/arm` 交叉编译 + `file` 校验；VM 内 scp 上板 + md5/字节数校验 + 回传；COM4 两种用法——`serial_run.py` 批量下发与 `plink -serial` 交互终端）、完整闭环 10 步命令、串口铁律与故障速查表、会话隔离现状与降级方案、纪律清单。
  2. 更正 `S02-core基础设施操作手册.md` §7 的串口调用：脚本名 `serial_send.py` → `serial_run.py`，并写明真实调用形式（位置参数 `COM4 115200 cmds.txt out.txt`，命令一行一条），指向新文档 §7 以免两处再漂移。
  3. `AGENTS.md` 关联文档表登记新文档。
- **验证结果**：纯文档改动，未动代码，按规则 4 第 4 条不写代码说明。文中所有平台事实均取自已入库的实测记录（`~/.ssh/config`、`bsp-capability.md` §0/§4/§5、`环境搭建/cmake安装步骤.md` §1）或模板文件实读（`CMakeLists.txt`、`cmake/toolchains/tina-arm.cmake`），未凭空编造；对无法确认的两处（板端 busybox 是否编入 `md5sum`、VM 是否装有 `sshpass`）已如实标注为"若不可用则退回…/未确认"，符合规则 5。另查明 **`-DIVSBOX_LOG_SYSLOG=ON` 目前并不存在**（工程 `CMakeLists.txt` 只有 `IVSBOX_BUILD_TESTS` / `IVSBOX_WERROR`），属 S02 步骤 4 才新增的开关，已在文中标注，避免照抄后误判"开关没生效"。
- **规则 8 合规性（含工具故障记录）**：本次改动在隔离 worktree `.stint/wb-devloop`（分支 `stint/wb-devloop`）内完成，未在 main 工作副本直接编辑。**但 git-stint 本机再次失效**：`git stint list` / `start` 报 `Error: git rev-parse failed: spawnSync git EBUSY` 与 `Error: Not inside a git repository`，而同目录 `git status`/`git rev-parse --show-toplevel` 均正常，加沙箱与绕过沙箱结果一致 → 判定为 git-stint 0.6.1 在本机 spawn git 失败。故改用等价的手工 `git worktree add .stint/<会话名> -b stint/<会话名>` 达成同样的物理隔离（`.stint/` 已被 `.gitignore` 忽略），收尾方式同规则 8（会话内 commit → 合回 main → 删 worktree 与分支）。此降级方案已写进新文档 §9，供另一 AI 复用。
- **规则 8 例外说明**：`AGENTS.md` 属规则 8 例外条款 1（规则文件自身），其改动随本次会话一并提交。

---

## 2026-09-23 17:58

- **修改文件**
  - `ivsbox-v5/docs/操作手册/S02-core基础设施操作手册.md`（新建）
  - `AGENTS.md`（关联文档表新增"操作手册"一行）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：用户要求提供 S02（core 基础设施）的详细操作手册，并指定放入新文件夹、新文档（用户主动提出新建，符合规则 7 第 2 条）。
- **处理方式**：依据开发列表 §3-S02 与 `bsp-capability.md` 回填结论编制 11 节操作手册：S02 定位与六件小事现状表；平台事实修正（日志接 syslogd 而非 logd、配置路径 `/opt/ivsbox/config/` 而非 `/data`）；决策 D1（按用户硬性约束不引入 cJSON，配置改 key=value 直接字符串解析，验收项"JSON 往返"改为"配置往返"）；实现步骤 1~6（CRC8/CRC16、SPSC 环形缓冲、配置原子写 tmp→fsync→rename→fsync(目录)、syslog 后端、已有三项核对、main 接线）；5 项单测方案与 CMake 追加代码；主机侧（VM `build/host` + ctest）与板端（scp + COM4 + logread）验证命令；收尾固定动作、验收清单与常见坑速查。同时在 AGENTS.md 关联文档表登记新文档。
- **验证结果**：纯文档新增，未动代码，按规则 4 第 4 条不写代码说明；手册中 S02 本身标记"未实现"，待后续会话按手册执行后回写实现记录。
- **规则 8 合规性**：本次改动在会话 `trae-s02man` 的 worktree 内完成；AGENTS.md 属规则 8 例外条款 1（规则文件自身），随会话一并提交。

---

## 2026-09-23 17:50

- **修改文件**
  - `ivsbox-v5/docs/bsp-capability.md`（§0~§3.1 回填盘点结论、§4~§6 更新主机侧与完成标志、新增 §7 实现记录）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：M0 的 BSP 能力清单此前全部为"待填"（2026-09-23 17:42 条遗留项）。开发列表明确"不填完这张表，后面写代码就是在赌"，且板端 rootfs 实测与架构假设不符，必须尽快落档。
- **处理方式**：以板端 `bsp-survey.sh` 实测输出（856 行）为准逐表回填：新增"§0 一页结论"；§2 七张表填"实际结果/结论"列并新增 §2.8 补充事实表（lighttpd/postgresql 已装、CAN×2、ADB/telnet 攻击面、板载时钟 1970 等）；§3 三条红线判定（①无 ffmpeg→选 GStreamer 路线；②不触发；③半触发→nice 弱隔离+看门狗）并新增 §3.1 五项架构差异记录（/data→/opt+/mnt/UDISK、logd→syslogd、procd→SysV+自管、ipk→tarball、uhttpd→lighttpd）；§4 主机侧四项由"cmake 不存在"更新为实测确认值；§5 标记完成并指向环境搭建文档；§6 五项完成标志全部勾选；新增 §7 实现记录（规则 1），如实保留两项待办：板间链路口/GPS 口/460800 待硬件确认、GStreamer 插件清点待执行。
- **验证结果**：全部结论均有原始盘点输出对应（关键行已摘入"实际结果"列）；三项交叉验证一致——板端 libc（ld-2.25.so）= 工具链 sysroot libc = `ivsboxd` 实机运行成功；`/opt`/`/mnt` 写探测 OK；`/dev/watchdog` 存在。纯文档改动，按规则 4 第 4 条不写代码说明。
- **规则 8 合规性**：本次改动在会话 `trae-bsp` 的 worktree 内完成，未动用例外条款。
- **清理（规则 6）**：按 17:42 条约定的删除条件执行——VM 侧 `/tmp/bsp-survey.txt`、板端 `/tmp/bsp-survey.sh` 与 `/tmp/bsp-survey.txt`、本机 `%TEMP%\bsp-survey.txt` 均已删除（原始证据已消化进本文档各表）；板端 `/tmp/ivsboxd` 保留（后续 S02 联调复用，tmpfs 重启自清）。

---

## 2026-09-23 17:42

- **修改文件**
  - `ivsbox-v5/docs/IVSBox-功能开发列表.md`（§10 实现记录：S01 置为"已实现"）
  - `.gitignore`（仓库根，忽略 `.embeddedskills/`）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：S01（交叉编译与上板跑通）完成验收，按规则 1 回写实现记录。同时串口调试技能在工作区生成了工具状态目录 `.embeddedskills/`（`config.json` 等，含 COM 口记忆），属本机工具状态而非工程文件，需排除出版本控制。
- **处理方式**：开发列表 §10 中 S01 行改为"已实现 / 2026-09-23 17:42"，说明列写明工具链与验收要点；`.gitignore` 追加 `.embeddedskills/`。
- **验证结果**：完整链路实测——① 天嵌编译 VM（192.168.2.115，`arm-linux-gnueabi-` gcc 7.3.1，sysroot glibc 2.25）上 `cmake` 配置 + 全量构建成功；② VM `scp` 推送 `ivsboxd`（20740 字节）到开发板（192.168.2.105，TQ113，Buildroot 2019.02.1，内核 5.4.61-rt37 armv7l）`/tmp/`；③ 板端 libc 比对：`/lib/ld-linux.so.3 -> ld-2.25.so`，与工具链 sysroot 完全一致；④ 经 Windows 侧 COM4（CH340，115200）串口控制台执行 `/tmp/ivsboxd`，输出 `IVSBox 0.1.0 (ipc proto v1, cfg schema v1)`、`mono_clock_ready=yes`、skeleton 提示、`exit` 四行，`RUN_EXIT=0`。S01 验收标准（`file` 显示 ARM ELF + 板上版本行正常）全部满足。
- **顺带修正的认知**：板端 rootfs 实测为 **Buildroot 2019.02.1（glibc）**，非项目早期文档假设的 OpenWrt/musl；内核带 PREEMPT_RT 补丁。该事实影响 S02（logd）、S17（uhttpd）、S18（procd respawn）、S20（ipk 打包）的选型，待 `docs/bsp-capability.md` 回填时统一落档（盘点输出 856 行已在 VM `/tmp/bsp-survey.txt`，尚未取回整理）。
- **规则 8 合规性**：本次改动全部在会话 `trae-s01` 的 worktree 内完成，未动用例外条款。
- **清理（规则 6）**：结束占用 COM4 的"串口调试助手"进程（PID 21776，经用户确认）；`.embeddedskills/` 保留为本机工具状态、已加 `.gitignore`；VM 上 `/tmp/bsp-survey.txt` 与板端 `/tmp/bsp-survey.sh`、`/tmp/ivsboxd` 暂留（盘点文档与后续联调仍要用，删除条件：`docs/bsp-capability.md` 回填完成后清理）。

---

## 2026-09-23 17:18

- **修改文件**
  - `AGENTS.md`
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：`ivsbox-v5/docs/环境搭建/cmake安装步骤.md` 是上一轮**在未征得用户同意的情况下新建**的，违反规则 7 第 2 条；同时该条还要求新建文档必须登记进 `AGENTS.md` 的"关联文档"表，此前未登记。经用户确认，选择"保留该文档并补登记"（另两个备选为：并入 `docs/bsp-capability.md` 的"主机环境准备"一节后删除独立文件；直接删除）。
- **处理方式**：在 `AGENTS.md` 的"关联文档"表末尾新增一行——"编译环境搭建 → `ivsbox-v5/docs/环境搭建/`"，并在说明列写明其定位（编译机环境搭建步骤、S01 前置）与保留依据（规则 7 第 2 条、经用户确认）。被保留文档的内容本身未做任何改动。
- **验证结果**：`AGENTS.md` 的改动仅为新增一行（表格原有 9 行不变）；`ivsbox-v5/docs/环境搭建/cmake安装步骤.md` 内容未变动，其 §6"执行记录"章节满足规则 1 对新建 `.md` 须有实现记录的要求。本次为纯文档改动，按规则 4 第 4 条不写代码说明。
- **规则 8 合规性**：本次改动落在规则 8 的例外范围（`AGENTS.md` 允许在 main 工作副本直接改），但 `AGENTS.md` 是与 WorkBuddy 的共用文件、并发编辑风险高，故仍选择在会话 `trae-docreg` 的 worktree 内完成，**未动用例外条款**。
- **清理（规则 6）**：本次无新增临时文件；沿用上轮结论，VM 侧 `/tmp` 与工作副本 `.stint/` 均无残留。

---

## 2026-09-23 17:16

- **修改文件**
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：补齐 17:15 条留下的"遗留"项——该条的全量构建用的是 `-DCMAKE_C_FLAGS="-D_GNU_SOURCE"` **等价参数**，而不是改动落地后的文件本身。按规则 2 第 5 条不得回头修改历史条目，故另起本条记录真实改动落地后的复核结果；同时按规则 6 第 5 条登记本轮清理动作。
- **处理方式**：在编译 VM（`tqt113`）上从远端**全新 clone** `ef5a334`，先确认 `ivsbox-v5/CMakeLists.txt` 第 46~48 行即为本次改动，再用**不带任何额外 `-D` 参数**的标准命令配置并全量构建，最后清理 `/tmp` 与 `main` 工作副本下的会话残留目录。
- **验证结果**：`git rev-parse --short HEAD` = `ef5a334`；`cmake -S . -B /tmp/ivs-verify-build -DCMAKE_TOOLCHAIN_FILE=cmake/toolchains/tina-arm.cmake -DIVSBOX_BUILD_TESTS=OFF -DCMAKE_BUILD_TYPE=Release` → `configure_exit=0`；`cmake --build /tmp/ivs-verify-build -j` → `build_exit=0`，`ivsboxd` / `ivsbox-media` / `ivsbox-web` / `ivsbox-updater` / `ivsbox-provision` 及两个静态库全部产出；`file ivsboxd` = `ELF 32-bit LSB executable, ARM, EABI5 version 1 (SYSV), dynamically linked, interpreter /lib/ld-linux.so.3, for GNU/Linux 3.2.0`，大小 20620 字节。至此 17:15 条的遗留项关闭。**上板运行仍未进行**，`docs/IVSBox-功能开发列表.md` 中 S01 状态保持"未实现"。本次为纯文档改动，按规则 4 第 4 条不写代码说明。
- **规则 8 合规性**：本条目在会话 `trae-verify` 的 worktree 内完成，未动用例外条款。
- **清理（规则 6）**：① VM 侧 `/tmp/ivs-verify`、`/tmp/ivs-verify-build`（本轮）与 `/tmp/ivs-build-check`（上轮）均已删除，`/tmp` 下无 `ivs*` 残留；② 会话 `trae-build` 合并后遗留的**空目录** `.stint/` 已删除——`git stint merge` 输出 `Session cleaned up.` 后该空目录仍在，属工具残留、非仓库跟踪内容，实测 `git worktree list` 与 `git branch --list 'stint/*'` 均已干净。

---

## 2026-09-23 17:15

- **修改文件**
  - `ivsbox-v5/CMakeLists.txt`
  - `ivsbox-v5/docs/代码说明.md`
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：板端交叉构建在 `src/core/iv_log.c:43` 报 `implicit declaration of function 'clock_gettime'` 与 `'CLOCK_MONOTONIC'未声明`，因 `-Werror` 直接编译失败。根因是顶层 `CMakeLists.txt` 的 `CMAKE_C_EXTENSIONS OFF` 会传 `-std=c11`，严格 ISO C 下编译器定义 `__STRICT_ANSI__`，glibc 的 `features.h` 因此不暴露 `clock_gettime` / `CLOCK_MONOTONIC` 等 POSIX 声明。`src/hal/iv_clock.c:23` 使用同一组接口，S03 要用的 `epoll` / `timerfd` / `signalfd` / `accept4` 同属这一类，需一次性解决而不是逐个文件打补丁。
- **处理方式**：在顶层 `CMakeLists.txt` 的 `add_compile_options(-Werror)` 之后插入 `if(UNIX) add_compile_definitions(_GNU_SOURCE) endif()`；因其位于各 `add_library` / `add_executable` / `add_subdirectory` 之前，对 `ivcore` / `ivhal` / `ivsboxd` 及 `media` / `web` / `updater` / `tools` / `tests` 全部生效，Linux 主机侧同样受益，Windows(MinGW) 主机不受影响。未改动任何源文件。按规则 4 在 `docs/代码说明.md` 追加 17:15 条目详述取舍与验证。
- **验证结果**：① 逐文件复现与修复——`arm-linux-gnueabi-gcc -std=c11 -Wall -Werror` 编译 `iv_log.c` / `iv_clock.c`，不带宏时报使用者遇到的同一错误，加 `-D_GNU_SOURCE` 后无输出、退出码 0；② 全量交叉构建——在 VM 上以 `-DCMAKE_C_FLAGS="-D_GNU_SOURCE"`（与本次改动等价的合成命令行）配置并编译，`configure_exit=0`（日志含 `ivsbox: sysroot = /opt/EmbedSky/Tina/arm-buildroot-linux-gnueabi/sysroot`、`The C compiler identification is GNU 7.3.1`），`build_exit=0`，`libivcore.a` / `libivhal.a` / `ivsboxd` / `ivsbox-media` / `ivsbox-web` / `ivsbox-updater` / `ivsbox-provision` 全部产出；`file ivsboxd` = `ELF 32-bit LSB executable, ARM, EABI5 ... interpreter /lib/ld-linux.so.3`，大小 20716 字节。**遗留**：②用的是等价参数而非改动后的文件本身（VM 上 clone 早于本次改动），需使用者重新构建复核一次；上板运行尚未进行，`docs/IVSBox-功能开发列表.md` 中 S01 状态保持"未实现"。
- **规则 8 合规性**：本次改动（含共用文档）全部在会话 `trae-build` 的 worktree 内完成，未动用例外条款。
- **清理（规则 6）**：VM 侧构建目录 `/tmp/ivs-build-check` 已删除（`/tmp` 无 `ivs*` 残留）；逐文件验证的输出丢弃到 `/dev/null`，未落盘；本机无新增临时文件。

---

## 2026-09-23 17:00

- **修改文件**
  - `ivsbox-v5/docs/修改记录.md`（本文件，补齐 16:56 条未涵盖的合并 / 清理 / 推送结果）
- **问题 / 目的**：16:56 条写到"会话内改动 + 冲突检查"为止，**合并、清理、推送这三步当时还没发生**；按规则 2 第 5 条不得回头修改历史条目，故另起本条补齐结果。同时把两条实测事实固化，避免后续重复试错：① **`git stint merge` 是"合并 + 清理"一步完成**——合并到 main 后直接输出 `Session cleaned up.`，不需要再单独执行 `git stint end`；② 多 AI 同库并发下 "push 前先 fetch" 是必须动作——本次准备推送时发现远端已是最新，原因是 WorkBuddy 在同一仓库并发提交并推送了 `a8d9abf docs(env): 新增 TQT113 编译 VM 的 cmake 安装步骤文档`，它的 push 把本会话的合并提交一并带上了远端。
- **处理方式**：会话内 `git stint commit` 得 `3e3900d8`（2 文件）→ `git stint squash` 压合为 `1af7397e` → 回 main 执行 `git stint merge --session trae-flow-verify`，输出 `Merged 'stint/trae-flow-verify' into 'main'.` 与 `Session cleaned up.` → 核查会话残留 → 删除遗留的空目录 `.stint/` → `git push` 并用 `git ls-remote origin refs/heads/main` 与本地 `rev-parse` 三方核对。
- **验证结果**：① `git show HEAD:AGENTS.md` 尾部已含新增两行（"会话隔离配置 `.stint.json`"、"Claude Code 适配 `.claude/`"），合并内容正确；② 提交链为 `e7ca721 → 1af7397 → a8d9abf`，`git rev-parse HEAD`、`git rev-parse origin/main`、`git ls-remote origin refs/heads/main` 三者一致（均 `a8d9abf`），其中 `1af7397` 是本次会话压合后的提交、`a8d9abf` 是 WorkBuddy 的并发提交；③ 清理核查：`git stint list` = `No active sessions`、`git worktree list` 仅剩 `F:/debug/T113_TCP`、`git branch --list 'stint/*'` 为空、`.stint/` 空目录已删除、`git status -sb` 为 `main...origin/main` 无差异；④ 会话内 `git stint conflicts --session trae-merge-evidence` 无重叠告警。本次为纯文档改动，按规则 4 第 4 条不写代码说明。**规则 8 合规性**：本条记录本身同样在会话 `trae-merge-evidence` 内完成，未动用例外条款。

---

## 2026-09-23 16:56

- **修改文件**
  - `AGENTS.md`（"关联文档"表补登记 `.stint.json`、`.claude/` 两项）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：`.stint.json` 与 `.claude/` 已在 16:49 入库，但规则 7 第 2 条要求共用文件必须登记进 `AGENTS.md` 的"关联文档"表，此前遗漏——新加入的一方从表里看不到会话隔离配置放在哪。同时按用户要求，用一次**真实任务**把规则 8 的完整流程跑通验证（开工 → 会话内改动 → 登记与冲突检查 → 提交 → 压合 → 合并回 main → 清理 → 推送），而不是只做空跑。
- **处理方式**：`git fetch origin` 确认与 `origin/main` 一致后 `git stint start trae-flow-verify`（基线 `e7ca7212`），**全部改动都在 `.stint/trae-flow-verify/` worktree 内进行**，main 工作副本只用于拉取与合并：在"关联文档"表末尾补两行（`.stint.json` 记为本规则 8 的仓库级配置、`.claude/` 记为 Claude Code 侧 hook 与适配说明）；`git stint track` 登记改动文件，`git stint conflicts` 检查与其他会话的重叠；`git stint commit` 后 `git stint squash` 压成单个干净提交，再合并回 main、`git stint end` 清理会话、`git push` 推送。
- **验证结果**：① `git stint start` 输出 `Branch: stint/trae-flow-verify`、`Worktree: F:\debug\T113_TCP\.stint\trae-flow-verify`，`git stint list` 显示基线 `e7ca7212`、提交数 0；② `git stint track AGENTS.md ivsbox-v5/docs/修改记录.md --session trae-flow-verify` 后 `git stint status` 的 `Pending files` 正确列出这两个文件；③ `git stint conflicts --session trae-flow-verify` 返回 `No other active sessions`（当前无其他会话，符合预期）；④ `git status` 在 worktree 内显示 `M AGENTS.md`，main 工作副本未被改动。**合并、清理与推送的结果另见紧随其后的 17:0x 补充条目**（规则 2 第 5 条：不改历史条目，另起一条记录）。本次为纯文档改动，按规则 4 第 4 条不写代码说明。

---

## 2026-09-23 16:50

- **修改文件**
  - `AGENTS.md`（规则 8 的"例外"条款由一句话扩为三类清单，用户确认后写入）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：上一条（16:49）在 main 工作副本直接提交了仓库根的工具配置文件，但依据的只是规则 8 例外条款的"精神"——原文只写了"`AGENTS.md` 规则本身的修改、以及紧急修复"两类，**没有覆盖工具配置文件这一类**。规则与实践不一致会让后续每一方都要重新判断，用户已确认把该情形正式写进规则。
- **处理方式**：把规则 8 末尾的"**例外**"改写为三类明确清单，并要求每类都必须在规则 2 的记录中写明原因：① **规则文件自身**（`AGENTS.md` 本文件的修改）；② **仓库根的工具配置文件**（`.stint.json`、`.claude/` 等），并写明理由——这类文件由工具从 main 根目录直接读取，且 `adopt_changes: "always"` 会把 main 上未提交/未跟踪文件 stash 后搬进新会话 worktree，放进会话反而会让配置被搬离 main、导致另一方 hook 与隔离策略瞬时失效；③ **紧急修复**，须说明"为什么不能先开会话"。
- **验证结果**：`AGENTS.md` 规则 8 例外条款现为编号 1/2/3 三类，与 16:49 记录中的实际做法（第 2 类）一一对应，规则与操作不再有解释差；检查表未变（仍为"是否已开会话、已清理"一项自查），例外情形凭本条款豁免。本次为纯文档改动，按规则 4 第 4 条不写代码说明；按规则 8 第 1 类例外（规则文件自身）在 main 工作副本完成。推送后 `git status -sb` 为 `main...origin/main` 无差异。

---

## 2026-09-23 16:49

- **修改文件**
  - `.stint.json`（新增入库，git-stint 仓库级配置）
  - `.claude/settings.json`（新增入库，Claude Code 的 git-stint hooks 配置）
  - `.claude/rules/git-stint.md`（新增入库，Claude Code 侧的工具适配说明）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：这两个配置文件此前只以**未跟踪状态**躺在 main 工作副本里，有两个后果：① 每次 `git status` 都出现 `??`，与规则 6 第 4 条"提交前自检不得出现本次任务之外的未跟踪文件"冲突；② **更关键**——`git-stint` 默认 `adopt_changes: "always"`，会把 main 上未提交/未跟踪的文件 stash 后搬进新会话 worktree（工具 README "Adopting Uncommitted Changes" 明确说明）。也就是说只要任一方执行 `git stint start`，`.claude/` 与 `.stint.json` 就会被搬离 main，WorkBuddy 的 Claude Code hook（`PreToolUse`/`Stop` 调 `git-stint-hook-*`）会瞬时失效。纳入版本库后，每个 worktree 随检出自带该配置，这个隐患消除，同时两个 AI 共用同一套隔离策略。
- **处理方式**：① 先确认内容再入库——`.stint.json` 与工具自带默认值一致（`main_branch_policy: "block"`、`force_cleanup: "prompt"`、`adopt_changes: "always"`、`shared_dirs`/`shared_files`/`post_create` 均为空），故入库的作用是**把策略显式化并共用**，不改变行为；`.claude/rules/git-stint.md` 内容明确指向 `AGENTS.md` 规则 8（会话前缀 `trae-`/`wb-`、必须显式带 `--session`），属工具适配层，**不是平行规则文本**，不违反规则 7 第 3 条；② 按规则 3 第 5 条**逐文件 add**（不使用 `git add -A`）后提交推送。
- **验证结果**：`git status --short` 提交前仅两个 `??` 条目，提交后不再有未跟踪文件；`git ls-files .stint.json .claude` 能列出上述三个文件即入库成功；推送后 `git status -sb` 为 `main...origin/main` 无差异。本次为配置入库、无代码改动，按规则 4 第 4 条不写代码说明。**例外说明（规则 8）**：本次改动对象是仓库根的**工具配置文件**，由两个工具从 main 根目录直接读取；若放进会话 worktree，反而会因 `adopt` 机制把配置从 main 搬走、直接影响另一方的 hook，故按规则 8 例外条款的精神在 main 工作副本直接完成，原因记录于此。

---

## 2026-09-23 16:47

- **修改文件**
  - `AGENTS.md`（新增规则 8"用 git-stint 会话隔离，禁止在 main 工作副本直接改文件"，检查表同步）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：WorkBuddy 已装好 `git-stint` 但**只有工具、没有规则**（未定义会话命名、开工步骤、收尾流程，也没跑过任何会话）。需求是"两个 AI 不要同时处理同一个文件"，仅靠规则 7 的人工约定不够：git 对同一文件被两方同时改写没有自动合并能力，后写方会直接覆盖。需要把会话隔离固化成可执行流程，并实测验证工具真的可用。
- **处理方式**：① 用三个一次性会话做**端到端实测**（`trae-smoke-test` / `trae-flow-test` / `trae-ov-a` + `wb-ov-b`），验证 `start` 建独立分支 `stint/<会话>` 与 worktree `.stint/<会话>/`、`track` + `conflicts` 能检出文件重叠、`commit`/`log`/`squash`/`status` 正常、`abort` 能彻底清掉分支与 worktree；② 依据实测结果在 `AGENTS.md` 新增规则 8：会话命名前缀（TRAE 用 `trae-`、WorkBuddy 用 `wb-`）、开工四步（fetch 对齐 → `start` 并 cd 进 worktree → `track` 登记改动 → `conflicts` 查重叠，有重叠即停下问用户）、收尾四步（commit → squash → fetch 后合并回 main → `end` 清理并 push），并写明共用文档冲突时"双方记录都保留、按时间倒序"；③ 记录两条实测限制：多会话并存时 Windows 上必须显式带 `--session <会话名>`（`cd` 进 worktree 也不自动识别），`install-hooks` 只适配 Claude Code、TRAE 需手动执行；④ 例外条款：`AGENTS.md` 自身修改与紧急修复允许在 main 工作副本做，但须在记录中写明原因。
- **验证结果**：`git stint conflicts --session trae-ov-a` 实测输出 `Overlap with session 'wb-ov-b': shared.txt`，**重叠检测有效**；`git stint squash -m` 实测把 1 个提交压合为新提交 `c0b12625`；三个测试会话全部 `abort` 后 `git stint list` 显示 `No active sessions`、`git worktree list` 只剩 `F:/debug/T113_TCP`、`git branch --list 'stint/*'` 为空、空目录 `.stint/` 已删除，main 工作副本未被测试触碰（规则 6 无残留）。本次为纯文档改动，按规则 4 第 4 条不写代码说明。**例外说明**：本次改的是 `AGENTS.md` 规则本身（规则 8 例外条款），故在 main 工作副本直接完成，未开会话。另：本地此前积压的 WorkBuddy 提交 `329347f`、`3692125` 已按用户确认随本次一并推送。

---

## 2026-09-23 16:28

- **修改文件**
  - `.gitignore`（补充 `.stint/` 忽略规则）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：本项目由多个 AI 助手（WorkBuddy、TRAE 等）并行操作同一仓库，此前仅靠"单写者约定 + 写入前读取最新内容"（规则 7 第 4、5 条）来防冲突，属人工约定、无工具级隔离。引入 `git-stint` 做会话级隔离：每个 AI 会话自动获得独立分支 + worktree（`.stint/<session>/`），改动自动跟踪、退出/崩溃自动 WIP 提交，main 分支不受污染，可 squash 出干净提交。需把其 worktree 目录 `.stint/` 排除出版本控制。
- **处理方式**：① 全局安装工具：`npm install -g git-stint`（v0.6.1，零运行时依赖，纯 Node.js 内建模块）；② 在仓库根 `.gitignore` 追加 `.stint/`；③ 工具以 `git stint <子命令>` 方式调用（`start` / `list` / `commit` / `squash` / `pr` / `merge` / `end` 等）。
- **验证结果**：`git-stint --version` 输出 `0.6.1`；`--help` 列出全部子命令，安装位置 `C:\Users\Think\AppData\Roaming\npm\git-stint.cmd`。环境满足前置要求：node v24.14.0、npm 11.9.0、git 2.55.0（支持 worktree）。**尚未在仓库执行 `git stint install-hooks`，也未创建任何 session**，需用户确认后再启用。本次为工具安装与环境配置，未产生仓库代码改动，按规则 4 第 4 条不写代码说明。

---

## 2026-09-23 16:25

- **修改文件**
  - `.gitignore`（新增忽略规则 `.workbuddy/`、`.trae/`）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：本机多个 AI 助手会在项目目录下建各自的工作目录（WorkBuddy 建 `.workbuddy/`，TRAE 可能建 `.trae/`），用于存放本地记忆、会话缓存等**非仓库内容**。这些目录此前未被忽略，导致每次 `git status` 都出现未跟踪提示，干扰规则 6"提交前自检不得出现本次任务之外的未跟踪文件"的执行。
- **处理方式**：在仓库根 `.gitignore` 的"编辑器 / 工具缓存目录"区块下，追加 `.workbuddy/` 与 `.trae/` 两行，并注明用途；同时验证 `.history/`、`.arts/` 等已有规则未受影响。
- **验证结果**：`git check-ignore -v ".workbuddy/memory/2026-09-23.md"` 命中 `.gitignore:26:.workbuddy/`；`git status --short` 中未跟踪文件清空，不再出现 `.workbuddy/`。本次为纯配置改动，按规则 4 第 4 条不写代码说明。

---

## 2026-09-23 16:09

- **修改文件**
  - `AGENTS.md`（新增规则 7"多 AI 协同必须共用同一份文档与同一套代码"，检查表与关联文档同步）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：本项目由多个 AI 助手（WorkBuddy、TRAE 等）操作**同一个项目目录** `f:\debug\T113_TCP`。此前规则未约束"多方是否共用同一份文档与规则"，存在两种风险：① 各 AI 各自维护平行副本 / 独立规则文件，导致口径分叉、结论互不可信；② 无防覆盖约定，双方同时改同一份文档时后写者会整段冲掉先写者的记录（本机存在 `.history/`，说明文件被编辑器持续跟踪，覆盖风险真实存在）。需要一条硬规则：**无论哪一方处理问题，都用同一份文档、同一套规则、同一份代码**。
- **处理方式**：在 `AGENTS.md` 新增规则 7，明确七条：①文档唯一性——列出双方共用的权威文档清单（`AGENTS.md` / `docs/修改记录.md` / `docs/代码说明.md` / `docs/IVSBox-功能开发列表.md` / 根目录两份架构文档）；②新建文档必须先向用户说明并获同意，新建后登记进"关联文档"表；③规则与代码同源（同一套 `AGENTS.md`、`.clang-format`、`.gitattributes`，不得各自引入本地约定）；④防覆盖——每次写入前必须先读目标文件最新内容，在其基础上增量修改，严禁基于过期内容整段覆盖；⑤单写者约定——同一时刻尽量只有一个 AI 写同一份文档，确需并行时只改互不重叠章节并在规则 2 记录中写明范围；⑥同步基线——开工前 `git fetch origin` 对齐 `main`，收尾按规则 3 提交推送；⑦冲突处理——发现文件已被另一方改动时先停下并向用户说明，禁止强行覆盖。检查表追加一条自查项；"关联文档"表补充 `AGENTS.md`（规则 7 权威规则文件）与 `.clang-format`（风格基线）两行；文件头部"建立日期"括注增补"规则 7"。本次为纯文档改动，按规则 4 第 4 条不写代码说明。
- **验证结果**：写入前已重新读取 `AGENTS.md` 全文，确认与上一轮核对结果一致（TRAE 未改动），本次为增量追加、未覆盖既有内容；`git status` 仅显示 `AGENTS.md` 与 `ivsbox-v5/docs/修改记录.md` 两处修改，无未跟踪临时文件。提交并推送后 `git status -sb` 为 `main...origin/main` 无差异。

---

## 2026-09-23 15:55

- **修改文件**
  - `AGENTS.md`（新增规则 6"处理问题结束后不得遗留任何临时文件"，检查表同步）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
- **问题 / 目的**：此前规则只约束"改完要登记、要提交"，但没有约束收尾时工作区的干净程度 —— 调试脚本、临时日志、备份文件、被注释掉的死代码容易留在工程里，逐步污染仓库并干扰后续判断。需要一条硬规则：**处理问题结束后只保留最终文件**。
- **处理方式**：在 `AGENTS.md` 新增规则 6，明确五条：①临时物清单（临时脚本/一次性测试程序/调试日志/抓包与 dump/`*.bak`·`*.tmp`·`*~` 等中间文件/被替换的旧文件）任务结束前必须清理；②禁止用"注释掉"代替删除，回溯靠 git 历史；③构建产物不入库并确认被 `.gitignore` 覆盖；④提交前用 `git status` 自检，确需保留的调试证据要写明用途与删除条件；⑤清理动作写进规则 2 的修改记录。检查表追加一条自查项。
- **验证结果**：本次任务自身按新规则自查 —— `git status --short` 仅显示 `AGENTS.md` 一处修改，**无任何未跟踪的临时文件**；本机无新增备份/日志/脚本残留。清理动作本身：本次无临时文件需要删除（规则 6 第 5 条：无清理项，故无删除记录）。提交并推送后 `git status -sb` 为 `main...origin/main` 无差异。

---

## 2026-09-23 15:53

- **修改文件**
  - `AGENTS.md`（增补：规则 3 补全远端地址、新增规则 4、规则 5，检查表与关联文档同步）
  - `ivsbox-v5/docs/代码说明.md`（新增，规则 4 的交付载体）
  - `ivsbox-v5/docs/修改记录.md`（本文件，登记本次修改）
  - `.gitattributes`、`ivsbox-v5/.clang-format`、`ivsbox-v5/docs/bsp-capability.md`（从远端恢复，本机工作副本此前缺失）
- **问题 / 目的**：① 本机工作副本 `F:\debug\T113_TCP` 与远端严重脱节：`git log` 无任何提交、无 `origin` 远端，且缺少 `AGENTS.md`、`.gitattributes`、`.clang-format`、`bsp-capability.md` 等远端已有文件，导致规则 1~3 在本机无载体、被规则引用的两处文档指向空目标。② 需按需求新增三项规则：推送目标远端显式固定为 `https://github.com/ppp1234lll/T113_S4`；每次写代码后交付一份"干了什么事、解决什么问题"的代码说明；T113/天嵌平台问题必须先查天嵌官方语雀资料。
- **处理方式**：先 `git remote add origin` + `git fetch origin main` 对齐远端基线（`c6964a20`），把工作副本重置到远端 main 并从远端取回本机缺失的三个文件（**不删除远端已有内容**）。随后在原有 `AGENTS.md` 上**增量**修改而非重写：规则 3 标题扩为"修改成功后必须提交并推送 git"，写入远端地址并新增"推送失败必须告知用户"一条；新增规则 4"代码说明"（落点 `ivsbox-v5/docs/代码说明.md`）与规则 5"天嵌官方资料优先"（语雀入口 `https://embedsky-rd.yuque.com/nqqg7d/fztngl`）；检查表与关联文档表同步更新。修改记录文件按用户确认**沿用本文件（`ivsbox-v5/docs/修改记录.md`）**，即需求口径中的 `xiugaijilu.md`，不另建同名文件。本次为纯文档改动，按规则 4 第 4 条不写代码说明。
- **验证结果**：`git status` 显示仅 `AGENTS.md`、`ivsbox-v5/docs/修改记录.md` 修改 + `ivsbox-v5/docs/代码说明.md` 新增；`docs/bsp-capability.md` 等远端文件已恢复（不再出现 `D` 状态）。提交并推送后 `git status -sb` 显示 `main...origin/main` 无差异，远端 `refs/heads/main` 与本地 HEAD 一致。

---

## 2026-09-23 16:57

- **修改文件**
  - `ivsbox-v5/docs/环境搭建/cmake安装步骤.md`（新增，含子目录 `docs/环境搭建/`）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：编译 VM（天嵌 TQT113 配套 Ubuntu 22.04，192.168.2.115）**没有安装 cmake**，而 `ivsbox-v5/CMakeLists.txt` 要求 `cmake_minimum_required(VERSION 3.16)`，S01 的 ARM 构建无法配置。需要一份可直接照做的安装步骤，并说明本机实测的版本、源与前置条件，避免照搬通用教程踩坑。
- **处理方式**：先在该 VM 上做只读探测（软件源、候选版本、安装模拟、磁盘、外网、sudo 是否免密、构建工具与交叉工具链是否在 PATH），再据实测结果写成文档，存放于新目录 `docs/环境搭建/` 下，与其它文档分离。文档含：结论、实测数据表、安装步骤、装完后的 S01 衔接命令、常见问题与卸载、参考来源、执行记录（规则 1）。
- **验证结果**：探测结果——apt 源为清华镜像 jammy；`apt-cache policy cmake` 候选 3.22.1-1ubuntu1.22.04.2（满足 ≥3.16）；`apt-get -s install cmake` 模拟通过；根分区 37G 可用；到镜像 443 端口可达；`sudo` 需要密码；`make` 4.3、`ninja` 1.8.2、`arm-linux-gnueabi-gcc` 均已在 PATH。**安装本身由使用者执行，尚未验证**，文档 §6 执行记录中相关行状态为"待执行"。
- **清理（规则 6）**：本次为只读探测加文档写入，VM 侧未生成任何临时文件（`apt-get -s` 为模拟，不写盘）；本机无新增临时文件。

---

## 2026-09-23 16:15

- **修改文件**
  - `ivsbox-v5/cmake/toolchains/tina-arm.cmake`
  - `ivsbox-v5/docs/代码说明.md`（新建，规则 4 的权威记录文件）
  - `ivsbox-v5/docs/修改记录.md`（本文件）
- **问题 / 目的**：S01 开工前需确认交叉工具链形态。实测发现 `tina-arm.cmake` 写死的平台事实与实际环境不符——脚本默认前缀 `arm-openwrt-linux-`、sysroot 为 `${TINA_SDK_ROOT}/out/t113/staging_dir/target`，且未导出 `TINA_SDK_ROOT` 时直接 `FATAL_ERROR`；而实际环境（天嵌 TQT113 编译 VM，192.168.2.115）工具链前缀为 `arm-linux-gnueabi-`（gcc 7.3.1 Linaro / 软浮点），sysroot 为 `/opt/EmbedSky/Tina/arm-buildroot-linux-gnueabi/sysroot`，且**不存在任何 Tina SDK 的 staging_dir**。照原脚本执行必然配置失败。
- **处理方式**：① `IVSBOX_CROSS_PREFIX` 默认值改为 `arm-linux-gnueabi-`（覆盖入口保留）；② 新增 `IVSBOX_SYSROOT` 缓存变量，默认指向实测 sysroot；③ sysroot 解析改为"`IVSBOX_SYSROOT` 优先 → `TINA_SDK_ROOT` 次之 → 都不可用则告警并退回工具链自带 sysroot"三级，删除 `TINA_SDK_ROOT` 的致命检查；④ 命中 sysroot 时设置 `CMAKE_SYSROOT` 与 `CMAKE_FIND_ROOT_PATH`，并删除下方重复设置 `CMAKE_FIND_ROOT_PATH` 的一行。同步按规则 4 新建 `docs/代码说明.md` 记录改动细节。
- **环境侧动作（非仓库文件）**：为目标机在 `C:\Users\Think\.ssh\config` 追加 `Host tqt113`（192.168.2.115 / embedsky / 密钥 `~/.ssh/id_ed25519`）；因 TRAE 沙箱禁止在 `.ssh` 下新建文件（skill 脚本的自动备份步骤失败），改为手工追加，备份写在 `%TEMP%\ssh-config.bak-20260923-162139`。已把该主机指纹并入 `~/.ssh/known_hosts`，临时文件 `%TEMP%\ivs_known_hosts` 已删除。
- **验证结果**：VM 上 `arm-linux-gnueabi-gcc -print-sysroot` 输出与脚本默认值完全一致；用该前缀 + sysroot 编译最小程序退出码 0，`file` 显示 `ELF 32-bit LSB executable, ARM, EABI5, dynamically linked, interpreter /lib/ld-linux.so.3`。**未验证**：cmake 尚未在该 VM 安装，`ivsboxd` 的 ARM 构建与上板运行待补。已记录待确认项：sysroot 内 glibc 为 2.25，与天嵌《应用开发手册》所述"需要 glibc 2.34"不一致，板端 rootfs 的 libc 版本需上板比对。
- **清理（规则 6）**：VM 侧 `/tmp/ivs_probe`、`/tmp/ivs_probe.c`、`/tmp/ivsbox-verify`（scp 半途失败留下的目录）均已删除；本机临时 known_hosts 已删除。
- **更正说明**：写入 `docs/代码说明.md` 时，误按"新建文件"整篇写入，覆盖了该文件原有的文件头与"记录格式模板"段。发现后已从 `HEAD:ivsbox-v5/docs/代码说明.md` 取回原文并还原，最终内容保留原有文件头与模板，本次条目按模板格式追加在其后。该文件在本仓库的历史条目未受影响。

---

## 2026-09-22 17:45

- **修改文件**
  - `ivsbox-v5/docs/bsp-capability.md`（新增，从 `F:\AI设计\Workbuddy\linux\ivsbox-v5` 归档回工作副本）
  - `ivsbox-v5/scripts/bsp-survey.sh`（新增，同上）
  - `.gitattributes`（新建，源码与脚本强制 LF）
- **问题 / 目的**：两件事。① 工作副本迁移到 `F:\debug\T113_TCP\ivsbox-v5` 时漏带两个文件，而它们是 S01 开工的前置资产：`bsp-capability.md` 是架构文档 §14 要求的"立项初期 BSP 能力清单"，`bsp-survey.sh` 是生成该清单的板端盘点脚本；缺失会导致 BSP 能力（libc 类型、串口、看门狗、ffmpeg、可写分区）无据可依。② 仓库本机 `core.autocrlf=true` 且无 `.gitattributes`，`bsp-survey.sh` 检出到 Windows 工作副本后行尾会变成 CRLF，一旦 scp 到板子上执行，`/bin/sh` 会因 `^M` 报错，脚本直接跑不起来。
- **处理方式**：从旧脚手架目录原样复制这两个文件到工作副本，保持内容不变、避免与旧副本分叉；在仓库根新增 `.gitattributes`，规定 `*.c/*.h/*.sh/*.cmake/*.md` 等一律 LF、`*.bat/*.cmd` 保留 CRLF、二进制不转换。
- **验证结果**：`scripts/bsp-survey.sh`（7923 字节）、`docs/bsp-capability.md`（6710 字节）已就位；`.gitattributes` 生效后 `git add` 不再报 LF/CRLF 转换告警。下一步在板端执行 `./bsp-survey.sh > /tmp/bsp-survey.txt 2>&1` 回填 §2 各表。

---

## 2026-09-22 17:40

- **修改文件**
  - `.gitignore`（新建，仓库根忽略规则）
  - `ivsbox-v5/docs/IVSBox-功能开发列表.md`（新增第 10 章"实现记录"）
- **问题 / 目的**：项目无版本控制仓库，规则 3 无法执行；主开发文档缺少实现进度登记章节，规则 1 无落点。
- **处理方式**：在 `f:\debug\T113_TCP` 初始化 git 仓库，添加远端 `https://github.com/ppp1234lll/T113_S4`，建立根 `.gitignore` 排除 `build/`、`.history/`、`.arts/`、`*.exe`、`_*.txt` 等构建产物与工具缓存；在主开发文档末尾新增"实现记录"表格，列出 S01~S20 任务清单及当前状态（仅工程骨架为已实现）。
- **验证结果**：本地提交 `0d7c7dd` 已推送，远端 `refs/heads/main` 与本地 HEAD 一致；`git status -sb` 显示 `main...origin/main` 无差异。
- **更正**：上一条（17:35）记录标题原误填为 17:45，晚于本条，导致倒序排列错误，已于本条处理时更正为实际创建时间。

---

## 2026-09-22 17:35（原误填 17:45，于 17:40 更正）

- **修改文件**
  - `AGENTS.md`（新建）
  - `ivsbox-v5/docs/修改记录.md`（新建，本文件）
- **问题 / 目的**：项目此前没有成文的开发规则，功能实现无回写记录、修改无历史追踪、修复后无 git 备份，导致进展不可追溯、问题无法复现。
- **处理方式**：在项目根目录建立 `AGENTS.md`，写入三条强制规则：①功能实现后回写 `.md` 文档的实现记录；②每次修改登记本文件（文件、问题、时间）；③问题修复后提交并推送 git 远端。同时建立本文件作为规则 2 的权威记录载体。
- **验证结果**：`AGENTS.md` 与 `ivsbox-v5/docs/修改记录.md` 创建成功，规则内容与本条记录一致。
