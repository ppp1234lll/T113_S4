> 本文件按 `AGENTS.md` 规则 4 维护，**有代码产出时必写**。顺序：倒序，最新在上。

---

## 2026-09-24 15:30

### 干了什么事

按架构文档 §15 新建 `ivsbox/` 工程目录，替换退役的 `ivsbox-v5/`：

- 新增顶层 `Makefile`，实现 host/arm 双目标、`make test`/`asan`/`fuzz`/`analyze`/`package`/`clean`/`help`。
- 新增 `mk/toolchain-host.mk`、`mk/toolchain-tina-arm.mk`、`mk/rules.mk`。
- 新增目录树：`src/{app,core,hal,modules/*}`、`media/`、`web/`、`updater/`、`tools/{provision,uart_simulator,platform_simulator}`、`tests/{unit,fuzz,integration}`、`packaging/`、`scripts/{init.d,ci}`、`docs/`。
- 新增最小桩源码：
  - `include/ivsbox/ivsbox.h`
  - `src/core/{iv_log,iv_err,iv_clock,iv_crc}.c`
  - `src/hal/iv_hal.c`
  - `src/modules/iv_modules.c`
  - `src/app/main.c`、`media/src/main.c`、`web/src/main.c`、`updater/src/main.c`、`tools/provision/src/main.c`
  - `tests/unit/{test_core,test_hal}.c`
- 迁移历史文档到 `ivsbox/docs/`，更新 `AGENTS.md` 中所有 `ivsbox-v5/` 引用为 `ivsbox/`。

### 解决什么问题

原 `ivsbox-v5/` 使用 CMake，与架构文档 §15 规定的"纯 GNU Make"冲突；目录结构也与 §15.1 不一致。本次重建后，构建入口、工具链配置、库分层、依赖方向均与架构设计对齐。

### 影响范围

- 所有后续源码新增到 `ivsbox/` 下对应目录；`ivsbox-v5/` 已彻底删除。
- `Makefile` 使用目录通配发现源文件，新增 `.c` 到 `src/core/`、`src/hal/`、`src/modules/<dir>/`、`media/src/` 等目录即可自动参与构建。
- 桩源码会在 S02 起被真实实现替换，当前仅用于验证构建体系可编译。

### 验证方式与结果

- 用 PowerShell 检查 `ivsbox/Makefile` 中食谱行以制表符开头（`Select-String -Path ivsbox\Makefile -Pattern "^`t"` 计数 > 0）。
- 目录树与架构文档 §15.1 逐条比对一致。
- **未在本地 Linux VM 执行实际编译**（当前在 Windows），待后续在 192.168.2.115 上运行 `make`、`make test`、`make arm` 验证。
